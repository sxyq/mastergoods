# 历史 154 环境说明

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 系统设计 |
| 当前状态 | historical-only |
| 适用端 | 多端 |
| 依据源码 | 无当前源码（154 已退役） |
| 依据测试 | `testing/README.md`（Historical Execution Snapshot） |
| 依据证据 | `testing/.artifacts/` 中 154 历史证据、`docs/05_测试与验收/验收证据/b11/backend/20260630-1338-124-154-readonly-status.md` |
| 最后核对 | 2026-08-20 |

## 一、退役声明

`154.217.241.207` 已完全退役。154 的部署资料、运行配置、数据库副本和测试结果只作为历史资料处理，**不属于当前线上环境，不参与当前通过率、发布验收或运行状态判断**（`testing/README.md`）。

## 二、历史 154 环境快照（historical-only）

| 项 | 历史值（154） |
|---|---|
| users | 5 |
| sessions | 5 |
| stores | 1 |
| store_memberships | 1 |
| agent_conversations | 18 |
| agent_messages | 29 |
| agent_run_audits | 18 |
| agent_run_audit_events | 41 |
| agent_drafts | 3 |

以上数据是 154 历史 baseline，**不得回写成当前 8220 结果**（`testing/README.md`）。

## 三、历史运行特征（154，仅作追溯）

- 154 容器实际 LLM 配置曾为 `enabled=true`、`model=gpt-5.6-luna`、`wire_api=responses`（2026-07-26 重部署后）。
- 真机 `d715a3a4`（型号 `25010PN30C`）曾在 154 环境执行冷启动与性能取证。
- 业务 Agent 原生工具链曾在 154 通过真实回归（普通问题文本；图表问题由模型选择 `sales_trend_lookup -> result_visualization`）。
- provider 隔离探针的标准 `function_call_output` 续轮在 154 返回 HTTP 400（保留为独立已知问题，需在 8220 独立验证）。

## 四、历史边界（不得复用的旧数据）

| 数据 | 说明 |
|---|---|
| conversation 57/58/59/64 | 154 环境会话证据（历史 only） |
| 30 轮只读长历史测试 | 临时 conversation 64 已删除 |
| 2026-07-27 性能快照 | 单次快照，不能替代重复 p95/Perfetto/Macrobenchmark |

## 五、归档位置

- 历史测试证据：`testing/.artifacts/`（154 相关 run-id 目录）。
- 154 部署资料：已从仓库删除（`deploy/154/` 在 git status 中为 D）；历史说明见本目录 `90_历史归档/154服务器历史资料说明.md`。

## 对应实现

- 后端代码：不适用（154 已退役）
- Android 代码：不适用
- iOS 代码：不适用
- Web 代码：不适用
- Agent 代码：不适用

## 对应接口

- 接口路径：不适用
- 请求模型：不适用
- 响应模型：不适用
- SSE 事件：不适用

## 对应测试

- 历史证据：`testing/.artifacts/`、`docs/05_测试与验收/验收证据/b11/`
- 历史说明：`testing/README.md`（Historical Execution Snapshot）

## 当前限制

- 未完成内容：无（历史已归档）
- Blocked 内容：无
- Deferred 内容：无
- historical-only 内容：本页全部内容均为 historical-only
