# 数据库运行边界

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 系统设计 |
| 当前状态 | 已完成 |
| 适用端 | 后端 |
| 依据源码 | `application.yml`、`application-prod.yml`（spring.datasource）、`db/migration/` |
| 依据测试 | `ZhihuijiBackendApplicationTests.java` |
| 依据证据 | `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md` |
| 最后核对 | 2026-09-01 |

## 一、数据库配置

| 配置 | 值 |
|---|---|
| 类型 | PostgreSQL 15 |
| URL | `DB_URL`（env） |
| 用户/密码 | `DB_USER` / `DB_PASSWORD`（env） |
| Flyway | enabled，`classpath:db/migration`，V42 |
| Hibernate | `ddl-auto=validate`，`open-in-view=false` |
| 连接池 | Hikari：`DB_POOL_SIZE:20`、`DB_POOL_MIN_IDLE:5`、connection-timeout 30000ms、max-lifetime 1800000ms |

## 二、运行边界（8220 基线）

当前数据库只读计数（2026-08-31）：用户 3、管理员账号 1、门店 2、session 6、Agent 会话 0、Agent 消息 0、Agent 审计 0、草稿 0。

规则：

1. 不创建临时账号、门店、业务夹具或猜测密码。
2. 测试不得建立长期种子库，只在执行时补最小夹具并记录创建/清理（`testing/README.md`）。
3. 业务汇总计数为 products=693、customers=83、suppliers=358、sale_orders=3846、purchase_orders=7、finance_records=2661、accounts=5、payments=904、inventory_snapshots=693、inventory_ledger=0、media_assets=0。

## 三、迁移边界

- 结构变更必须通过 Flyway 迁移（V1-V42），禁止 Hibernate 自动建表（validate）。
- 2026-09-01 目标环境已从 V41 应用 V42；发布记录见 `testing/admin/20260830-production-deployment.md`。
- 迁移校验：`testing/.artifacts/2026-08-01-server-agent-eval/wave0-isolated-migration-rerun-20260801T023947+0800/isolated-migration-sql-validation.md`。

## 对应实现

- 后端代码：`application*.yml`、`db/migration/`
- Android 代码：`core/database/`（Room 本地镜像）
- iOS 代码：不适用
- Web 代码：不适用
- Agent 代码：不适用

## 对应接口

- 接口路径：无
- 请求模型：无
- 响应模型：无
- SSE 事件：无

## 对应测试

- 迁移验证：`testing/.artifacts/2026-08-01-server-agent-eval/`
- 基线：`testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md`

## 当前限制

- 未完成内容：新鲜库/已有数据迁移复验、PostgreSQL 查询计划和生产保留策略演练
- Blocked 内容：真实 PostgreSQL EXPLAIN、跨范围 HTTP 和 Android Agent 全链路尚未执行
- Deferred 内容：无
- historical-only 内容：154 数据库副本
