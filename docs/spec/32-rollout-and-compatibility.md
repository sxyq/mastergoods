# 32 发布与兼容

## 规则

| 项目 | 状态 | 说明 |
|---|---|---|
| /v1 兼容 | 新版已做 | 先保留 |
| /v2 单据域首批 | 新版已做 | 销售/采购/付款三条首批路由已建立 |
| /v2 商品与伙伴域首批 | 新版已做 | 商品、分类、单位、客户、供应商、分组、联系人首批路由已建立 |
| /v2 商品域第三阶段扩域 | 新版已做 | 已建立价格层级、商品-供应商关系两组路由，并升级 `/v2/products` 读写模型 |
| /v2 同步与导入首轮合同 | 待验证 | 已建立 `/v2/sync/*` 与 `/v2/import-jobs/*`，仍待客户端联调与后台执行器补齐 |
| 数据迁移 | 新版待做 | 增量式演进 |
| 旧数据导入到服务器 | 新版待做 | 不纳入第一阶段 |
| 安卓新版 UI 切换 | 待验证 | 等 `/v2` 稳定后再做 |
| 本地 Java 21 编译验证 | 新版已做 | 之前缺匹配的 JDK 21，当前第三阶段代码已通过 `compileJava` 与 `compileTestJava` |
| 第二阶段 `/v2` 商品与伙伴测试补齐 | 新版已做 | 新扩域代码刚落地时仍缺迁移、service、controller、`/v1` 兼容回归验证；现已补齐 migration/service/controller/compatibility 测试，并修正联系人主摘要测试桩顺序，当前以 JDK 21 全量测试通过为验收基线 |
| 第三阶段商品域测试补齐 | 新版已做 | 已补 `V9` migration、`V2ProductPriceLevelService`、`V2ProductSupplierRelationService`、`V2ProductController`、`/v1` 兼容断言的测试代码 | 当前已通过本地 JDK 21 `gradlew test` |
| 第六阶段 B06 本地编译验证 | 新版已做 | 之前沙箱下 Gradle daemon 绑定本地 socket 受限，无法直接完成验证 | 已在本机 Gradle 8.7 + JDK 21 环境下完成定向回归测试执行 | 当前已确认跑通 `V2SyncServiceTest`、`V2ImportJobServiceTest`、`V2SyncImportControllerTest` |
| 第七阶段 B07 本地回归验证 | 新版已做 | media/agent 首轮落地后仍缺真实回归证据 | 已在本机 JDK 21 环境跑通 `V2MediaServiceTest`、`V2AgentConversationServiceTest`、`V2AgentMediaControllerTest` | 当前已确认 owner 维度 media/agent 首轮合同可编译且定向测试通过 |
| 第七阶段 B07 修复增强 | 待验证 | 首轮合同缺会话更新/删除、状态约束与级联删除 | 已新增 `PUT /v2/agent/conversations/{id}`、`DELETE /v2/agent/conversations/{id}`、`AgentConversationUpdateRequest` DTO；会话状态约束 `[active, closed, archived]`，草稿状态约束 `[active, archived]`；`closed/archived` 会话拒绝新消息；会话删除级联到消息与草稿（服务级 + DB 级 `ON DELETE CASCADE`，V14 迁移）；已增 `AgentMessageRepository.deleteAllByOwnerUserIdAndConversationId`、`AgentDraftRepository.deleteAllByOwnerUserIdAndConversationId` 批量删除方法；已移除废弃 DTO（`AgentMessageListResponse`、`MediaBindingListResponse`）；安卓端已补 `UpdateAgentConversationRequest`、`updateAgentConversationV2`/`deleteAgentConversationV2`、`updateConversation`/`deleteConversation`，契约测试已对 agent/media 全量端点做 HTTP 方法、路径与关键 query 参数断言 | 仍待真实工作台联调与端到端验证 |
| 第八阶段 B08 本地编译验证 | 新版已做 | 安卓 `/v2` 首轮承接代码落地后仍缺编译与契约证据 | 已在 `master-goods-android` 本机 Gradle 环境跑通 `:core:model:compileDebugKotlin`、`:core:network:testDebugUnitTest --tests com.zhihuiji.core.network.ZhihuijiV2ApiContractTest`、`:data:product:compileDebugKotlin`、`:data:customer:compileDebugKotlin`、`:data:supplier:compileDebugKotlin`、`:data:order:compileDebugKotlin`、`:data:finance:compileDebugKotlin`、`:data:sync:compileDebugKotlin` | 当前已确认 `product/partner/order/finance/inventory/sync-import/agent/media` 的 `/v2` model/network/repository 首轮承接可编译 |
| B08 非阻塞问题修复验证 | 新版已做 | B08 审核发现 6 个非阻塞问题 | 已修复：Filter 类补齐 `@Serializable`/`@SerialName`、agent/media API 方法名统一 `V2` 后缀、`AgentV2Repository.deleteDraft()`/`MediaV2Repository.deleteAsset()`/`deleteBinding()` 改用 `safeApiUnitCall`、`@Query` 参数名已验证与后端一致并加注释；已补齐 `V2ModelSerializationTest`、`SafeApiCallBehaviorTest`、`FinanceV2RepositoryTest`、`AgentV2RepositoryTest`、扩展 `ZhihuijiV2ApiContractTest`，其中 Repository 测试已直接调用真实仓储方法验证 API 委派链路 | 全部新增测试通过 |
| B06 稳定 cursor 分页 | 待验证 | 仅按时间戳分页会在同一 `updated_at` 跨页时漏数 | `/v2/sync` 使用稳定 cursor token，兼容旧数值 cursor | 代码与文档已同步，待真实客户端 pull/ack 联调验证 |
| B06 pull/ack 语义分离 | 待验证 | 如果 pull 即自动推进服务端游标，会让“已拉取未落库”状态不可回放 | `/v2/sync` 以 `ack` 作为服务端游标推进入口，pull 仅返回 `next_cursor` | 代码与文档已同步，待客户端按新语义联调 |
| B06 import job 状态门控 | 待验证 | 如果 retry/cancel 不受状态约束，会让导入任务语义漂移 | `/v2/import-jobs/*` 只允许 `failed/cancelled -> retry`、`pending/running -> cancel` | 代码、测试、文档已同步，待客户端按新语义联调 |
| B04/B05 审计缺陷回归修复 | 新版已做 | 审核发现 `pay_order` 幂等、退货数量、伙伴引用校验、inventory by-source 文档漂移问题 | 代码、测试、文档三侧一致收口 | 已完成源码修复、补 service/controller test，并在本机 Gradle 8.7 + JDK 21 环境完成定向回归 | 优先级高于继续扩 B06 |
| B09 feature 层 /v2 切换 | 待验证 | feature 层已完成首轮切换，但仍有局部混用 | assembleDebug + V2 契约测试通过 | `products/customers/suppliers/reports/dashboard/finance` 已大范围切入 `/v2`；本轮已补齐 `sales/purchases/payments` 编辑搜索到 `/v2` 主数据仓储，并让 `settings` 具备 `pull -> apply -> ack(next_cursor)` 手动同步链；`agent` 任务/通知、`auth`、媒体上传链、owner-aware Room 扩域缓存仍待后续补齐 |
| B11 验收入口与证据规范 | 新版已做 | 之前缺统一验收矩阵、证据目录和最小上下文复验入口 | 已新增 `docs/spec/41-b11-acceptance-matrix.md` 与 `tools/b11_acceptance_check.sh` | 该项只代表 B11 验收框架已建立，不代表真机/117/性能/安全发布完成 |
| B11 后端/Android 本地自动化 | 新版已做（定向覆盖） | 需要本轮真实命令输出作为证据，且不能把局部 Android 覆盖误写成全量数据层验证 | 已跑通 `backend-smoke`、`android-contract`、`android-assemble`、`android-assemble-release`、`backend-bootjar`；其后又补跑 `:data:product:testDebugUnitTest`、`:data:customer:testDebugUnitTest`、`:data:supplier:testDebugUnitTest`、`:data:order:testDebugUnitTest`、`:data:sync:testDebugUnitTest`，并在 2026-06-04 额外补了四条 UI 收口后的编译证据：1）` :feature:dashboard:compileDebugKotlin :feature:reports:compileDebugKotlin :feature:agent:compileDebugKotlin :app:compileDebugKotlin` 的定向 UI 编译；2）` :feature:products:compileDebugKotlin :feature:customers:compileDebugKotlin :feature:suppliers:compileDebugKotlin :feature:sales:compileDebugKotlin :feature:purchases:compileDebugKotlin :feature:payments:compileDebugKotlin :app:compileDebugKotlin` 的详情/编辑页诚实态定向编译；3）重新执行 `./tools/b11_acceptance_check.sh android-assemble` 的整包 debug 构建；4）重新执行 `./tools/b11_acceptance_check.sh android-assemble-release` 的整包 release 构建；证据见 `docs/acceptance-evidence/b11/backend/20260603-1709-backend-smoke.md`、`docs/acceptance-evidence/b11/android/20260603-1709-android-contract.md`、`docs/acceptance-evidence/b11/android/20260603-1709-android-assemble.md`、`docs/acceptance-evidence/b11/android/20260603-1730-android-assemble-release.md`、`docs/acceptance-evidence/b11/backend/20260603-1732-backend-bootjar.md`、`docs/acceptance-evidence/b11/android/20260603-2354-android-repository-delegation.md`、`docs/acceptance-evidence/b11/android/20260604-0915-android-ui-targeted-compile.md`、`docs/acceptance-evidence/b11/android/20260604-0227-android-ui-honesty-final-compile.md`、`docs/acceptance-evidence/b11/android/20260604-0930-android-assemble.md`、`docs/acceptance-evidence/b11/android/20260604-0945-android-assemble-release.md` | 以上 PASS 来自 dirty worktree，本轮应解读为“当前在途工作树上的本地/定向复验”；后端根目录无独立 `./gradlew`，脚本已改为可复用 `master-goods-android/gradlew -p <后端根目录>`；Android `android-contract` 仍主要强证 `core:model`、`core:network`、`data:agent`、`data:finance`，但 repository 委派层已额外补齐 `product/customer/supplier/order/sync` 的定向单测；2026-06-04 的新增证据只补强最新 UI 收口后的 Kotlin、debug assemble 与 release assemble 健康度，不等于真机或发布验收；同时仍有 AGP 8.5.2 对 compileSdk 35 的非阻塞提示 |
| B11 真机/117/性能/安全发布 | 待验证 | 不得用编译通过代替发布验收 | 真机截图、117 环境 smoke、性能稳定性记录、安全发布清单均需独立证据 | 当前本机已确认缺少 `adb` / `emulator`，见 `docs/acceptance-evidence/b11/android/20260603-1712-emulator-blocked.md`；同时已补 Android release 与 117 静态发布清单，见 `docs/acceptance-evidence/b11/security-release/20260603-1720-android-release-security-checklist.md`、`docs/acceptance-evidence/b11/backend/20260603-1720-117-release-static-checklist.md`，并补充 `assembleRelease` 与 `bootJar` 成功证据：`docs/acceptance-evidence/b11/android/20260603-1730-android-assemble-release.md`、`docs/acceptance-evidence/b11/backend/20260603-1732-backend-bootjar.md`；截图目录统一为 `docs/acceptance-evidence/b11/screenshots/`，性能与安全发布记录分别进入 `performance/`、`security-release/` |
