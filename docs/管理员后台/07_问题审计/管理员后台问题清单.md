# 管理员后台问题清单

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 问题审计 |
| 当前状态 | 问题台账已更新；环境项仍开放 |
| 适用端 | 管理员 Web、管理员 API、数据库和 Agent 观测 |
| 问题前缀 | `ISSUE-ADM` |
| 最后核对 | 2026-08-30 |

## 一、当前问题

| 编号 | 问题 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|
| `ISSUE-ADM-01` | 当前复用统一认证，尚无独立管理员登录/退出接口 | 真实后台会话流程仍需补齐 | 高 | 部分完成 |
| `ISSUE-ADM-02` | 观察员授权范围虽已由 `admin_scope_grants` 解析，真实跨范围 HTTP 尚未验证 | 需确认范围响应和审计行为 | 高 | 源码已完成；环境待验证 |
| `ISSUE-ADM-03` | V36 已增加 actor/store 字段，历史运行可能没有可靠值 | 历史详情需明确显示未知 | 高 | 源码已完成（历史可空） |
| `ISSUE-ADM-04` | V37 已建立管理员审计和读写记录，真实审计链尚未运行 | 需验证失败和查询自身审计 | 高 | 源码已完成；环境待验证 |
| `ISSUE-ADM-05` | 内容权限和 DTO 脱敏已实现，真实响应/日志/导出扫描尚未执行 | 需完成敏感字段验证 | 高 | 源码已完成；环境待验证 |
| `ISSUE-ADM-06` | 正式 Vue 页面、API 客户端和路由已创建，临时 React 原型仍为静态数据 | 真实 API、数据库和 SSE 页面验收未完成 | 高 | 源码已完成；环境待验证 |
| `ISSUE-ADM-07` | 管理员列表和聚合已有分页 Query，尚未做 PostgreSQL 计划验证 | 目标数据量下性能未知 | 中 | 部分完成 |
| `ISSUE-ADM-08` | 管理员事件补读和持久化读取已实现，真实断线/重连尚未运行 | 需验证事件不丢不重 | 中 | 部分完成 |
| `ISSUE-ADM-09` | 模型、工具配置和版本幂等接口已实现 | 真实配置刷新仍需验证 | 中 | 源码已完成；环境待验证 |
| `ISSUE-ADM-10` | 脱敏导出、范围快照、过期状态和保留策略已实现 | 真实文件清理和周期待验证 | 中 | 源码已完成；环境待验证 |
| `ISSUE-ADM-11` | 临时原型为 React，正式 Web 已按 Vue 页面实现 | 需完成浏览器核验和真实 API 页面测试 | 中 | 源码与构建已完成；浏览器待验证 |
| `ISSUE-ADM-12` | 目标环境、真实数据和 PostgreSQL 查询计划尚未验证 | 不能给出正式性能结论 | 中 | 阻塞 |

## 二、已确认的原型结论

临时原型的静态页面和交互已经完成本地核验：导航、指标卡、趋势图、资源环、运行表、工具域分布、调用日志、审计列表、运行详情抽屉和窄屏侧栏可见。它没有关闭上述正式系统问题。

## 三、问题分类

| 类别 | 对应问题 |
|---|---|
| 身份与权限 | `ISSUE-ADM-01`、`02` |
| 数据完整性 | `ISSUE-ADM-03` |
| 审计与隐私 | `ISSUE-ADM-04`、`05`、`10` |
| 功能实现 | `ISSUE-ADM-06`、`09` |
| 性能与可靠性 | `ISSUE-ADM-07`、`08`、`12` |
| 工程一致性 | `ISSUE-ADM-11` |

## 对应实现

- 当前原型：`Temp/grok-style-admin-preview/`
- 正式管理员模块：`Code/backend/src/main/java/com/zhihuiji/backend/{api/controller/admin,application/service/admin,infrastructure/security/admin,infrastructure/repository/admin}`
- 正式 Web 管理员模块：`Code/frontend/web/src/{pages/admin,features/admin,app/router/admin-routes.ts,shared/api/admin.ts}`
- Agent 审计与管理员迁移：`Code/backend/src/main/java/com/zhihuiji/backend/domain/entity/AgentRunAuditEntity.java`、`Code/backend/src/main/resources/db/migration/V35__administrator_identity_and_scope.sql` 至 `V41__admin_anonymous_denial_audit.sql`；目标数据库当前为 V40，V41 待发布。

## 对应接口

问题影响 `API-ADM-01` 至 `API-ADM-15`。

## 对应测试

解除条件和测试入口见 `管理员后台问题解除条件.md` 与 `../05_测试与验收/`。

## 当前限制

本清单已根据 2026-08-30 源码、迁移、管理员测试命令和临时原型核对。状态区分源码实现与真实环境证据；后续需逐条补充 HTTP、数据库、性能、SSE、生产部署和恢复证据。
