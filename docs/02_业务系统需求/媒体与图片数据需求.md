# 媒体与图片数据需求

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 业务系统需求 |
| 当前状态 | 已完成（媒体基础）；Deferred（图片给 Agent） |
| 适用端 | 后端 / 多端 |
| 依据源码 | `api/controller/v2/V2MediaController.java`、`application/service/v2/V2MediaService.java`、`domain/entity/MediaAssetEntity.java`、`MediaBindingEntity.java`、`infrastructure/storage/MediaStorageService.java`、`LocalMediaStorageService.java`、`application/service/v2/AgentImageService.java` |
| 依据测试 | `V2MediaServiceTest.java`、`V2AgentMediaControllerTest.java`、`AgentImageServiceTest.java` |
| 依据证据 | `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md` |
| 最后核对 | 2026-08-20 |

## 系统需求

### SR-MEDIA-001 媒体上传与绑定

| 字段 | 内容 |
|---|---|
| 需求编号 | SR-MEDIA-001 |
| 需求名称 | 媒体上传与绑定 |
| 业务目标 | 上传媒体资产并绑定到业务对象 |
| 参与角色 | 有媒体权限的成员 |
| 前置条件 | media_assets / media_bindings 表存在（V13） |
| 输入 | 媒体文件 |
| 处理规则 | `V2MediaService` + `MediaStorageService` 存储文件并建 media_assets / media_bindings |
| 输出 | 媒体资产记录 |
| 异常情况 | 文件大小超限（multipart 10MB） |
| 权限要求 | 认证后 |
| 涉及端 | 后端 / Android / iOS / Web |
| 对应接口 | `/v2/media/*`（`V2MediaController`） |
| 对应源码 | `application/service/v2/V2MediaService.java`、`infrastructure/storage/MediaStorageService.java`、`LocalMediaStorageService.java` |
| 对应测试 | `V2MediaServiceTest.java`、`V2AgentMediaControllerTest.java` |
| 验收标准 | 上传后可查询、绑定 |
| 当前状态 | 已完成（源码+测试）；8220 media_assets 为空 |

### SR-MEDIA-002 图片输入给 Agent

| 字段 | 内容 |
|---|---|
| 需求编号 | SR-MEDIA-002 |
| 需求名称 | 图片输入给 Agent |
| 业务目标 | 用户向 Agent 发送图片，模型直接分析 |
| 参与角色 | 有 agent:write 权限的成员 |
| 前置条件 | 多模态 Provider 与设计资料提供（当前未提供） |
| 输入 | 图片资产 ID（imageAssetIds） |
| 处理规则 | 源码存在 `V2AgentAiService.buildMultimodalDirectAnswer()`、`LongCatAnthropicClient.ImageInput`、`resolveImageInputs`；但 Provider 未启用，链路未验证 |
| 输出 | 多模态回答 |
| 异常情况 | 无（未执行） |
| 权限要求 | `agent:write` |
| 涉及端 | Agent / Android（uploadImage 源码存在） |
| 对应接口 | `POST /v2/agent/chat`（imageAssetIds 参数）、`POST /v2/agent/images/generate`（生图） |
| 对应源码 | `V2AgentAiService.buildMultimodalDirectAnswer()`、`AgentImageService.java`、`AgentImageProperties.java` |
| 对应测试 | `AgentImageServiceTest.java`；`testing/Agent/功能测试/TEST_PLAN.md`（Wave 3 multimodal） |
| 验收标准 | 图片链路完成 |
| 当前状态 | Deferred（等待 Provider 和设计资料；生图 provider 未配置） |

### SR-MEDIA-003 文本生图

| 字段 | 内容 |
|---|---|
| 需求编号 | SR-MEDIA-003 |
| 需求名称 | 文本生图 |
| 业务目标 | 文本生成图片 |
| 参与角色 | 有 agent:write 权限的成员 |
| 前置条件 | 生图 Provider 配置（当前未配置） |
| 输入 | 提示词 |
| 处理规则 | `AgentImageService.generate()`（`POST /v2/agent/images/generate`） |
| 输出 | 生成图片 |
| 异常情况 | 真机提交后未见生成态（已知问题 #6，历史 154 环境） |
| 权限要求 | `agent:write` |
| 涉及端 | Android（生图弹层存在） |
| 对应接口 | `POST /v2/agent/images/generate` |
| 对应源码 | `application/service/v2/AgentImageService.java`、`infrastructure/config/AgentImageProperties.java` |
| 对应测试 | `AgentImageServiceTest.java` |
| 验收标准 | 生图结果回写 |
| 当前状态 | Deferred（生图 provider 未配置） |

## 对应实现

- 后端代码：`application/service/v2/V2MediaService.java`、`AgentImageService.java`、`infrastructure/storage/`
- Android 代码：`data/agent/media/MediaV2Repository.kt`、`AgentChatViewModel.uploadImage()/generateImage()`
- iOS 代码：`Features/Settings/MediaAssetsView.swift`
- Web 代码：`entities/media/`
- Agent 代码：`V2AgentAiService` 多模态分支（Deferred）

## 对应接口

- 接口路径：`/v2/media/*`、`/v2/agent/images/generate`
- 请求模型：`api/dto/v2/media/V2MediaDtos.java`、`V2AgentDtos.AgentImageGenerateRequest`
- 响应模型：同上
- SSE 事件：无（媒体接口非流式）

## 对应测试

- 单元测试：`V2MediaServiceTest.java`、`V2AgentMediaControllerTest.java`、`AgentImageServiceTest.java`
- 功能测试：`testing/后端/功能测试/TEST_PLAN.md`
- 多模态：`testing/Agent/功能测试/TEST_PLAN.md`（Wave 3，Deferred）

## 当前限制

- 未完成内容：无（媒体基础完成）
- Blocked 内容：生图 provider 未配置
- Deferred 内容：图片输入给 Agent、图片结果展示、文本生图、多模态
- historical-only 内容：154 环境媒体与生图证据
