# 多端同步需求

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 业务系统需求 |
| 当前状态 | 已完成 |
| 适用端 | 后端 / Android / Web |
| 依据源码 | `api/controller/v2/V2SyncController.java`、`application/service/v2/V2SyncService.java`、`domain/entity/SyncCursorEntity.java`、`SyncChangeLogEntity.java`、`SyncTombstoneEntity.java`、`SyncOperationLogEntity.java` |
| 依据测试 | `SyncControllerTest.java`、`SyncServiceTest.java`、`V2SyncServiceTest.java`、`V2SyncImportControllerTest.java` |
| 依据证据 | `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md` |
| 最后核对 | 2026-08-20 |

## 系统需求

### SR-SYNC-001 多端数据同步

| 字段 | 内容 |
|---|---|
| 需求编号 | SR-SYNC-001 |
| 需求名称 | 多端数据同步 |
| 业务目标 | 多端数据增量同步 |
| 参与角色 | 所有端 |
| 前置条件 | 业务数据存在 |
| 输入 | 同步游标 |
| 处理规则 | `V2SyncService` 基于 sync_cursors 增量拉取；sync_change_log 记录变更；sync_tombstones 记录删除（V26/V27/V28 迁移） |
| 输出 | 变更数据、游标推进 |
| 异常情况 | 游标冲突 |
| 权限要求 | 认证后 |
| 涉及端 | 后端 / Android / Web |
| 对应接口 | `/v2/sync/*`（`V2SyncController`） |
| 对应源码 | `application/service/v2/V2SyncService.java`、`SyncCursorEntity.java`、`SyncChangeLogEntity.java`、`SyncTombstoneEntity.java`、`SyncOperationLogEntity.java` |
| 对应测试 | `V2SyncServiceTest.java`、`SyncServiceTest.java` |
| 验收标准 | 增量同步结果一致 |
| 当前状态 | 已完成（源码+测试） |

### SR-SYNC-002 导入任务

| 字段 | 内容 |
|---|---|
| 需求编号 | SR-SYNC-002 |
| 需求名称 | 导入任务 |
| 业务目标 | 历史数据导入（V22 迁移） |
| 参与角色 | 经营者 |
| 前置条件 | 导入文件存在 |
| 输入 | 导入任务 |
| 处理规则 | `V2ImportJobWorkerService` 异步处理 import_jobs |
| 输出 | 导入结果 |
| 异常情况 | 导入失败 |
| 权限要求 | 认证后 |
| 涉及端 | 后端 / Web |
| 对应接口 | `/v2/import-jobs/*` |
| 对应源码 | `application/service/v2/V2ImportJobService.java`、`V2ImportJobWorkerService.java` |
| 对应测试 | `V2ImportJobServiceTest.java`、`V2ImportJobWorkerServiceTest.java` |
| 验收标准 | 导入数据完整 |
| 当前状态 | 已完成（源码+测试） |

## 对应实现

- 后端代码：`application/service/v2/V2SyncService.java`、`V2ImportJobService.java`、`V2ImportJobWorkerService.java`
- Android 代码：`data/sync/`
- iOS 代码：`Core/Models/SyncModels.swift`
- Web 代码：`entities/sync/`
- Agent 代码：`SyncStatusLookupTool.java`

## 对应接口

- 接口路径：`/v2/sync/*`、`/v2/import-jobs/*`
- 请求模型：`api/dto/v2/sync/SyncV2Models.kt`（Android）
- 响应模型：同上
- SSE 事件：无

## 对应测试

- 单元测试：`SyncControllerTest.java`、`V2SyncServiceTest.java`、`V2SyncImportControllerTest.java`
- 功能测试：`testing/后端/功能测试/TEST_PLAN.md`

## 当前限制

- 未完成内容：iOS 同步链路待验证
- Blocked 内容：8220 无业务数据
- Deferred 内容：无
- historical-only 内容：154 环境同步证据
