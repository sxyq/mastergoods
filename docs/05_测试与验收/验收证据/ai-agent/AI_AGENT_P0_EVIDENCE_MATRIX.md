# AI Agent P0 Evidence Matrix

Status: `partial`
Updated: `2026-07-03`
Current baseline: current worktree adds provider-backed local `model_stream` evidence, repository owner-scope audit coverage, shared web helper de-dup, recent-message loading fixes, bounded audit-write observability, lifecycle-aware nav state collection, and focused code/test evidence.

This matrix maps `docs/spec/43-ai-assistant-requirements.md` requirement IDs to current evidence. It is an audit index, not a pass certificate. A row can only become `pass` when code evidence and runtime evidence both cover the full requirement scope.

Execution gates for future reviews live in `docs/spec/44-ai-assistant-review-gates.md`. The matrix remains the evidence index; `44` is the checklist that decides whether a review item can be marked `pass`, `partial`, or `fail`.

## Evidence Packages

| Evidence | Scope | Result |
|---|---|---|
| `docs/acceptance-evidence/ai-agent/20260608-agent-interface-evidence-index.md` | Local backend interface runs on `http://localhost:18080`, `SPRING_PROFILES_ACTIVE=local`, `AGENT_LLM_ENABLED=false` | `partial`: three business questions have interface-level SSE / audit evidence, but no provider `model_stream` and no full Android acceptance |
| `docs/acceptance-evidence/ai-agent/20260703-2250-41300-3xvunYP8-manual/` | 2026-07-03 local backend interface run on `http://127.0.0.1:18081`, sourced from current Codex config (`gpt-5.4-mini`) | `pass-for-interface`: raw SSE now includes real `answer_delta(delta_source=model_stream)`, audit returns `mode=tool_query_llm_streamed` / `llm_status=streaming` / `event_count=34`, and result blocks start after the first visible model delta |
| `docs/acceptance-evidence/ai-agent/20260703-codex-provider-model-stream.md` | Summary of the 2026-07-03 Codex-provider local run plus audit reconciliation | `pass-for-local-stream`: proves real provider stream is connected locally, but Android-paired evidence is still missing |
| `docs/acceptance-evidence/ai-agent/20260703-codex-provider-native-tool-planning.md` | Summary of the 2026-07-03 local non-stream provider run after fixing OpenAI-style non-stream URL/content-type compatibility | `partial-native-planning`: proves local native function-calling planning and LLM synthesis are back (`llm_status=available`, `plan_source=react_iterated`), but at least one real tool (`inventory_panorama_lookup`) still fails and Android-paired evidence is still missing |
| `docs/acceptance-evidence/ai-agent/20260614-0023-f6ec9e97-9190-46b5-a87e-962c311b296d/` | Latest 2026-06-14 local backend interface run, ASCII question `customer receivable`, verified against the currently running backend | `pass-for-interface`: fresh runtime evidence still shows visible `rule_summary` deltas before result blocks, SSE/audit reconciliation passes, and current backend truth is `llm_status=not_configured` rather than provider-backed `model_stream` |
| `docs/acceptance-evidence/ai-agent/20260613-2102-291a1607-fba8-43f4-a684-82cf406f05eb/` | 2026-06-13 local backend interface run, `question=客户应收情况`, `AGENT_LLM_ENABLED=false` | `pass-for-interface`: current worktree proves visible `answer_delta(delta_source=rule_summary)`, `result_block` follows the first visible answer delta, SSE / HTTP / audit reconcile, and current code/doc contract is no longer “answer only lands at answer_completed” |
| `docs/acceptance-evidence/ai-agent/20260613-2113-511f7551-5aa6-4f24-bd7f-b8ab9507d6d8/` | 2026-06-13 local backend interface run, ASCII question `customer receivable`, paired with device automation | `pass-for-interface`: rule-summary path still reconciles; used as the current backend-side counterpart for device automation and combined gating |
| `docs/acceptance-evidence/ai-agent/20260614-001813-device-ai-chat/12-conclusion.md` | Latest 2026-06-14 physical-device chat attempt using the new startup intent question path | `pass-for-device-ai-chat-evidence`: AI chat, visible `rule_summary` answer text, structured result blocks, evidence card and real `customer_receivable_lookup` tool anchor were all captured on-device |
| `docs/acceptance-evidence/ai-agent/20260613-235337-device-ai-chat/12-conclusion.md` | 2026-06-13 device evidence refresh after fixing the script's no-device blocker path | `blocked-no-device`: keep as historical blocker evidence showing the script now fails honestly when no single adb device can be inferred |
| `docs/acceptance-evidence/ai-agent/20260613-214513-device-ai-chat/12-conclusion.md` | Latest connected-device chat attempt on 2026-06-13 after auto-login and auto-open-chat | `partial-answer-not-visible`: device script honestly reached the AI chat shell and filled the question, but no real `/v2/agent/chat` request or visible answer/result/tool evidence was triggered before the device later disconnected |
| `docs/acceptance-evidence/ai-agent/combined-current/10-combined-gate-20260614.md` | Current combined chat gate using `20260614-0023-...` interface package and `20260614-001813-device-ai-chat` | `partial-combined-chat-missing-provider-stream`: interface status is `pass-for-interface`, device status is `pass-for-device-ai-chat-evidence`, stream ordering risk is gone, and the only remaining blocker is missing provider `model_stream` evidence on the current runtime |
| `docs/acceptance-evidence/performance/20260613-211757-ai-agent-performance/03-summary.md` | 2026-06-13 multi-question AI interface timing plus attached Android `gfxinfo` | `partial-rule-summary-performance`: provider `model_stream` remains `0/9`, no rule-summary ordering risk observed, Android frame stats attached, but end-to-end rule-flow performance still lacks a passing device/chat pair |
| `docs/acceptance-evidence/ai-agent/20260608-android-device-ai-flow/README.md` | Physical Android device AI flow with rule-summary mode | `partial`: clean AI home, streaming endpoint, Markdown-like answer, result blocks and RunTrace screenshots exist; provider stream and high-refresh performance still missing |
| `docs/acceptance-evidence/ai-agent/20260609-054441-device-ai-home/10-conclusion.md` | AI home evidence attempt | `blocked-by-locked-device`: keep as failed/partial attempt only |
| `docs/acceptance-evidence/ai-agent/20260609-0710-fc6a452b-7355-4209-8e61-a65d740313ac/` | Local backend interface run on `http://localhost:18080`, `SPRING_PROFILES_ACTIVE=local`, `AGENT_LLM_ENABLED=false`, question `库存和客户应收情况` | `partial`: two real tools executed, SSE/audit reconciliation passed, `answer_completed_status_consistency=pass`, workbench cleanliness passed; no provider `model_stream` and no Android rendering evidence |
| `docs/acceptance-evidence/ai-agent/20260609-0740-4a6bc46e-7e10-4b9c-b680-97f2cbee51b7/` | Local backend interface run on `http://localhost:18080`, `SPRING_PROFILES_ACTIVE=local`, `AGENT_LLM_ENABLED=false`, question `客户应收情况` | `partial`: `customer_receivable_lookup` executed, SSE/audit reconciliation passed, workbench cleanliness passed, `18-result-block-evidence.md` is `pass-for-interface` with `customer_count`, `top10_receivable_total`, tool source, query window and truncation markers; no provider `model_stream` and no Android rendering evidence |
| `docs/acceptance-evidence/performance/20260609-052957-backend-report-performance/04-conclusion.md` | Backend report performance sampling | `partial`: backend requests succeeded, but Android first-visible latency, UI tree and frame timing are not covered |
| `tools/capture_ai_chat_device_evidence.py --self-test` | Device AI chat evidence gate | `tooling-only`: self-test proves locked device, not-in-app, AI-home-only, missing answer, missing result block and missing tool anchors are not marked as pass; current script also supports `--auto-open-chat`, `--chat-entry-text`, `--poll-timeout-seconds`, `--raw-sse`, `--scenario safety-block|stop|clear`, and the honest `blocked-no-device` output path, but it still needs an unlocked connected device run to create real screenshots / UI tree |
| `tools/ai_agent_evidence_capture.sh cancel-test` | Interface-side cancel evidence gate | `tooling-only`: starts a real `/v2/agent/chat/stream`, waits for `run_id`, calls `/v2/agent/runs/{run_id}/cancel`, saves `05-cancel-*` files and writes `11-cancel-evidence.md`; self-test covers confirmed `run_cancelled` and honest already-completed / not-cancelled states, but Android stop / clear UI evidence is still required |
| `tools/ai_agent_evidence_capture.sh safety-test` | Interface-side safety block evidence gate | `tooling-only`: sends deterministic high-risk prompt `请删除所有数据`, captures raw SSE / audit, writes `12-safety-block-evidence.md`, and self-test rejects tool events after `safety_check_blocked`; still needs Android safety-block scenario evidence |
| `tools/ai_agent_combined_evidence_gate.py --self-test` | Combined interface + Android device evidence gate | `tooling-only`: refuses to promote interface-only or device-only packages to full acceptance; chat full pass requires interface `pass-for-interface`, Android `pass-for-device-ai-chat-evidence`, provider `delta_source=model_stream`, and raw SSE ordering with no `result_block` / `server_notice` before the first model delta; safety / stop / clear scenarios require matching interface and device scenario pass statuses |
| `docs/acceptance-evidence/ai-agent/combined-current/10-combined-gate.md` | Historical combined chat gate using interface package `20260609-0740-4a6...` and device package `20260609-075707-device-ai-chat` | `partial-combined-chat-evidence`: keep only as older partial evidence; current gating should use `10-combined-gate-20260613.md` |
| `docs/acceptance-evidence/ai-agent/20260609-075707-device-ai-chat/12-conclusion.md` | Device AI chat evidence attempt | `blocked-by-locked-device`: script captured lock-screen UI text and refused to claim AI chat, result block, Markdown, tool hint or streaming acceptance |
| `tools/ai_agent_performance_evidence.py --self-test` | AI agent multi-question performance evidence gate | `tooling-only`: parser self-test covers provider `model_stream`, result block ordering, non-model data-before-answer risk detection and Android `dumpsys gfxinfo` frame-summary parsing; still needs live provider stream plus attached device gfxinfo / first-visible evidence |
| `docs/acceptance-evidence/performance/20260609-090033-ai-agent-performance/03-summary.md` | Local H2 backend `/v2/agent/chat/stream`, `AGENT_LLM_ENABLED=false`, three business questions | `partial-rule-summary-performance`: 3/3 HTTP 200 and completed; p50 total response 494.3ms, p95 857.59ms; no result-before-answer risk in rule-summary mode; provider `model_stream` 0/3 and no Android frame evidence |
| `docs/acceptance-evidence/ai-agent/20260609-200838-message-window-performance/` | Code/test evidence for bounded AI chat history loading after baseline `6ccfec2` | `partial-code-and-test`: Android chat entry now uses `listRecentMessages(conversationId)` with `page=0`, `limit=80`; backend queries the recent message window by descending time and returns it in ascending timeline order; Android/data focused tests and backend focused tests passed; no connected adb device, provider stream, or frame evidence |
| `V2AgentAiServiceTest.streamEventsIncludeCompatibleEnvelopeMetadata` | SSE audit summary write optimization | `code-and-test`: event payloads still persist per SSE event and `event_count == events.size()`, while run summary save count is fixed to create + finish instead of per-event count updates |
| `V2AgentAiServiceTest.slowAuditEventWriteDoesNotBlockVisibleStreamPayloads` / `failedAuditEventWriteDoesNotFailStreamOrPoisonLaterEvents` | Async ordered audit event persistence | `code-and-test`: SSE payloads continue through `run_completed` while the first audit event write is blocked; finish waits for per-run audit drain; event count is derived from persisted event rows; a failed audit event write does not fail the stream or poison later event writes |
| `V2AgentAiServiceTest.streamPartialToolFailureEmitsToolFailedAndDoesNotInventMissingData` | Partial tool failure honesty | `code-and-test`: when `inventory_low_stock_lookup` succeeds but `customer_receivable_lookup` throws, the stream emits `tool_failed` with `tool_call_id`, `error_code`, `safe_message`, `query_window` and trace metadata; the final answer keeps the real low-stock result, explicitly says the receivable lookup failed, and does not invent zero receivable data or render a fake receivable block |
| `V2AgentConversationServiceTest` / `V2AgentMediaControllerTest` / `AgentV2RepositoryTest` / `ZhihuijiV2ApiContractTest` | AI conversation, message and draft list cap | `code-and-test`: `/v2/agent/conversations`, `/messages` and `/drafts` keep array response shape, accept optional `page/limit`, default to bounded repository queries, clamp oversized limit to 200, preserve legacy no-query Android calls, and Android network / repository layers can now pass `page/limit` through instead of being stuck on implicit first page defaults |
| `V2AgentAiServiceTest.productCatalogUsesRepositoryAggregatesForSummaryInsteadOfPageSample` / `V2AgentAiServiceTest.receivableAndPayableUseRepositoryAggregatesForTotalsInsteadOfTopPageSample` / `ProductRepositoryTest.productAggregatesAreOwnerScopedAndCountLowStockWithoutLoadingRows` | AI business summary correctness and performance | `code-and-test`: `product_catalog_lookup` keeps table rows paged but computes product count, stock total and low-stock count through owner-scoped repository aggregate queries; `customer_receivable_lookup` and `supplier_payable_lookup` keep Top10 ranking rows paged but compute total customer / supplier count and receivable / payable totals through repository aggregates, so KPI / evidence / rule-summary text no longer treats the first returned page as the full dataset |
| `AdminControllerProdProfileTest.localAdminDemoRoutesAreNotAvailableInProdProfile` / `localAdminControllerBeanIsNotRegisteredInProdProfile` | Production profile demo/admin route isolation | `code-and-test`: with active profiles `test,prod`, `/v1/admin/demo/seed` and `/v1/admin/agent/smoke` return 404 `Resource not found`, and `AdminController` is not registered in the application context; this is a prod-profile HTTP regression gate, not a deployed production environment proof |
| `AgentChatViewModelAnswerMergeTest.safetyBlockedResultStopsAssistantStreamingAndShowsHonestError` | AI safety-block terminal state after `e299d9e3` | `code-and-test`: `SafetyCheckBlocked` now stops assistant streaming, disables reveal, marks the message as an error, stores the safety reason, and writes `runTrace.safetyResult.passed=false`; still needs raw SSE, Android screenshot and UI tree evidence |
| `V2AgentAiServiceTest.cancelRunMarksActiveStreamCancelledAndEmitsRunCancelledEvent` / `cancelRunDoesNotPretendUnknownRunWasCancelled` / `cancelRunDoesNotCancelOtherOwnerActiveRun` / `AgentChatViewModelAnswerMergeTest.clearMessagesShouldCancelServerRunWhenStreamIsActive` / `AgentSseClientCancellationTest.chatStream_cancelsUnderlyingOkHttpCallWhileReadingResponseBody` | Stop generation, clear chat and SSE cancellation after `15a44a39` | `code-and-test`: stop / clear paths request server cancel when a run is active, unknown or cross-owner cancel is not reported as successful, active cancellation emits `run_cancelled`, and Flow cancellation reaches OkHttp both during execute and response-body read; still needs Android action capture, cancel HTTP/SSE, audit reconciliation and active-run cleanup evidence |
| `docs/spec/43-ai-assistant-requirements.md` / `docs/spec/44-ai-assistant-review-gates.md` / `AppNavGraph.kt` after `7b716845` | AI cancellation gates and lifecycle-aware navigation collection | `code-and-doc`: AGT-P0-020 / AGT-P0-021 are defined as explicit review gates, and top-level nav state uses lifecycle-aware collection for no-visual-change performance hygiene; still needs runtime evidence packages before any new row can be marked pass |

## P0 Requirement Matrix

| ID | Current status | Evidence | Missing proof / next action |
|---|---|---|---|
| AGT-P0-001 | `partial` | `43` code snapshot documents demo seed isolation and admin smoke is not the agent path; interface evidence uses `/v2/agent`; 2026-06-09 read-only risk review found no production AI chat path using mock/demo/sample/fake data; `LocalProfileGuardTest.localAdminAndDemoBeansAreNotRegisteredOutsideLocalProfile` proves admin/demo/local initializer beans are not registered under `prod` profile; `AdminControllerProdProfileTest` proves prod-profile MockMvc calls to `/v1/admin/demo/seed` and `/v1/admin/agent/smoke` are 404 and no `AdminController` bean is registered | Need deployed or production-like runtime evidence against the real DB / HTTP entrypoint proving demo seed cannot be called and tasks / notifications only come from real runs |
| AGT-P0-002 | `partial` | Interface packages show run ids, SSE, audit and `completed` status; Android device package includes backend run id and run audit | Need fresh end-to-end evidence after `7b716845`, including current Android UI and audit reconciliation |
| AGT-P0-003 | `partial` | Backend now has both keyword fallback and provider-backed native planning evidence: `20260703-codex-provider-native-tool-planning.md` proves local `/v2/agent/chat` can reach `llm_status=available` and `plan_source=react_iterated` after native tool calling + iteration; keyword fallback remains the honest downgrade path when the model path fails or is unavailable | Need Android-paired evidence, more than one live business question, and closure of tool-specific real failures such as `inventory_panorama_lookup` before this row can be promoted beyond `partial` |
| AGT-P0-004 | `partial` | Tool event contract and RunTrace screenshots expose tool sources, duration, returned counts and audit metadata; `V2AgentAiServiceTest.streamPartialToolFailureEmitsToolFailedAndDoesNotInventMissingData` proves a forced receivable tool failure emits `tool_failed`, preserves the successful inventory result, and does not replace the failed query with fake zero data or a fake result block | Need real SSE / DB audit package and Android screenshot proving the same `tool_failed` state is visible to users |
| AGT-P0-005 | `partial` | Unit tests now gate honest source labels instead of “no rule-summary delta”: backend emits `answer_delta(rule_summary)` for visible downgrade answers, `answer_delta(model_stream)` only for real provider streaming, and `server_notice` only for backend notice tails; `result_block` waits until the first visible answer delta in both model-stream and rule-summary paths, and partial model text is preserved with `stream_interrupted` when a provider stream fails after visible deltas; Android still keeps early `result_block` pending, deduplicates repeated blocks, uses short tool hints and keeps an honest “模型流式中断” header after interrupted model streams; `20260703-2250-41300-3xvunYP8-manual/` already proves a local provider-backed `model_stream` run with result blocks emitted after the first visible model delta | Need provider interruption / empty-stream evidence, Android screenshots / recording paired to the same provider-backed run, and current-device proof that UI labels `model_stream` / `server_notice` / `rule_summary` honestly |
| AGT-P0-006 | `partial` | Android device package includes expanded RunTrace screenshots; code has real `toggleRunTrace` | Need fresh `7b716845` device evidence with current collapsed / expanded behavior |
| AGT-P0-007 | `partial` | Interface and Android device evidence used `AGENT_LLM_ENABLED=false` and rule-summary mode | Need negative UI screenshots for model disabled, model error, stream failed and empty model output |
| AGT-P0-008 | `partial` | Code and docs state draft action is only archive / not executed | Need device evidence for draft dialog and no business write after archive |
| AGT-P0-009 | `partial` | Seed path review says demo AI artifact seed is removed / scoped; `AdminControllerTest.seedResetDoesNotDeleteNonDemoOwnerAgentArtifacts` proves local reset preserves non-demo owner task / notification rows; `AdminControllerProdProfileTest` proves prod-profile demo seed route is not routable through MockMvc | Need deployed or production-like DB/API evidence that task / notification rows are absent for new accounts until real runs or real backend jobs create them |
| AGT-P0-010 | `partial` | Interface evidence includes result blocks, evidence card and tool results; field-level evidence tests exist; `20260609-0740-4a6.../18-result-block-evidence.md` proves backend audit contains renderable field-level `evidence_card` items for `customer_count` and `top10_receivable_total` with tool source, query window and truncation markers; device chat evidence tooling now requires result/evidence anchors before it can pass | Need real UI screenshots tying answer numbers to evidence refs and query windows |
| AGT-P0-011 | `partial` | Three prior interface questions are `pass-for-interface`; current `20260609-0710-fc6a...` package adds a fresh rule-summary interface run with `inventory_low_stock_lookup` and `customer_receivable_lookup` tool evidence | Need current Android device evidence for the same questions and fresh rule / model mode labels |
| AGT-P0-012 | `partial` | Run audit endpoint and event audit are covered by tests and interface packages; `20260609-0710-fc6a...` and `20260609-0740-4a6...` show raw SSE and persisted audit events match seq/event_id/event_type and `status_consistency` passes | Need DB audit / SSE / HTTP / Android UI reconciliation in one package after latest commit |
| AGT-P0-013 | `partial` | Backend performance package exists for report APIs; `20260609-0710-fc6a.../11-latency.md` records AI interface timing: HTTP 200, total curl 72ms, audit duration 59ms, first event 16ms, first tool completed 31ms, answer completed 43ms, run completed 58ms, 2 tools, 6 result blocks and status consistency pass; `20260609-0740-4a6.../11-latency.md` records HTTP 200, total curl 56ms, audit duration 48ms, first event 16ms, first tool completed 30ms, answer completed 38ms, run completed 47ms, 1 tool, 4 result blocks; `20260609-090033-ai-agent-performance/03-summary.md` adds repeatable multi-question sampling on local H2 with 3/3 completed, p50 total 494.3ms, p95 total 857.59ms, and zero rule-summary result-before-answer inversions; `20260703-2250-41300-3xvunYP8-manual/02-raw-sse.log` now proves at least one real provider-backed `model_stream` sample on the local interface; run summary `event_count` now writes once at run terminal state; `agent_run_audit_events` writes are queued per run after SSE send through a bounded audit executor, finish/get audit wait for queue drain, DB `countByRunId` is the persisted summary source, and tests prove slow / failed / rejected audit writes do not block visible stream payloads or poison later event writes; audit loss is surfaced through V17 fields `emitted_event_count`, `audit_write_dropped_count`, `audit_write_failed_count`, `audit_lossy` and response `warnings` without setting business `error_code`, so persisted-vs-emitted gaps are explicit; Android `ZhihuijiV2Api` / `AgentV2Repository` expose optional `page/limit` for AI conversations, messages and drafts, and `AgentV2Repository.listRecentMessages` now fixes chat history entry loading to `page=0`, `limit=80`; backend `V2AgentConversationService` queries that bounded message window by descending time and returns it in ascending timeline order, with focused Android/data and backend tests recorded in `20260609-200838-message-window-performance`; `product_catalog_lookup` uses DB aggregates for product count, stock total and low-stock count while keeping table rows paged; receivable / payable tools use DB aggregates for full totals while keeping Top10 rows as ranking evidence | Need multi-sample provider-stream timing, interruption / empty-stream timing, Android first-visible timing, screen recording or frame stats, larger dataset / concurrent-run sampling, metrics, real DB latency evidence, and UI-level paged loading / infinite scroll if product needs history beyond the bounded first page |
| AGT-P0-014 | `partial` | Android device package says AI home is clean; latest locked-device package is explicitly blocked | Need unlocked fresh AI home screenshot and UI tree after current build |
| AGT-P0-015 | `partial` | Android device package shows answer, result blocks and RunTrace; current code differentiates roles, short tool pill and interrupted model-stream header state; `tools/capture_ai_chat_device_evidence.py` captures screenshot, UI tree, logcat and gfxinfo for chat evidence attempts | Need current recording or screenshots proving tool hints are transient, stream interruption is visible, and roles / result blocks remain visually distinct |
| AGT-P0-016 | `partial` | Markdown parser tests cover tables, code whitespace and links; Android device package shows readable paragraphs | Need true Markdown stress answer screenshots: title, list, table, quote, code, link, bold, italic and inline code |
| AGT-P0-017 | `partial` | ResultBlockRenderer tests cover invalid charts, unknown block, empty data and bad SSE frame | Need real or injected device screenshots for bad block, empty data, unknown block and chart blocks |
| AGT-P0-018 | `partial` | Workbench code and Android device package show clean entry; quick questions are filtered | Need fresh UI tree after current build and disconnected backend state screenshot |
| AGT-P0-019 | `partial` | Server cancel tests and Android repository path exist; `V2AgentAiServiceTest.cancelRunMarksActiveStreamCancelledAndEmitsRunCancelledEvent` proves an active stream can emit `run_cancelled` and persist `cancelled`; unknown and cross-owner cancel tests prove the backend does not pretend cancellation succeeded; Android `stopGeneration()` routes through `AgentV2Repository.cancelRun` and reports confirmed / unconfirmed / failed cancel feedback | Need Android stop click, cancel HTTP/SSE, `run_cancelled` or honest not-cancelled state, audit status and UI feedback in one evidence package |
| AGT-P0-020 | `partial` | `AgentChatViewModelAnswerMergeTest.safetyBlockedResultStopsAssistantStreamingAndShowsHonestError` proves `SafetyCheckBlocked` closes assistant streaming, marks the message as error, disables local reveal, stores the safety reason and writes `runTrace.safetyResult.passed=false` | Need raw SSE for a high-risk question, Android screenshot / UI tree proving the stop button disappears and the visible state is an error terminal state, plus RunTrace safety result evidence |
| AGT-P0-021 | `partial` | `AgentChatViewModelAnswerMergeTest.clearMessagesShouldCancelServerRunWhenStreamIsActive` proves clear chat reuses the active cancel path before clearing; `AgentSseClientCancellationTest.chatStream_cancelsUnderlyingOkHttpCallWhileReadingResponseBody` proves response-body read cancellation reaches OkHttp `Call.cancel()`; stop generation and clear chat no longer rely only on local UI state | Need clear / stop / leave-collection evidence with HTTP/SSE capture, connection-release proof, backend active-run cleanup or honest unconfirmed-cancel UI, and Android feedback screenshots |

## Required New Evidence Directories

- `docs/acceptance-evidence/ai-agent/provider-model-stream/`: local interface proof is now covered by `20260703-2250-41300-3xvunYP8-manual/`, `20260703-codex-provider-model-stream.md`, and `20260703-codex-provider-native-tool-planning.md`; remaining work is an Android-paired provider package with screenshots / UI tree / logcat plus tool-failure closure.
- `docs/acceptance-evidence/ai-agent/cancel-run/`: generated or refreshed with `tools/ai_agent_evidence_capture.sh cancel-test`; include cancel HTTP response, SSE `run_cancelled` or honest not-cancelled state, audit status, then add Android stop action, clear-chat action and UI feedback evidence.
- `docs/acceptance-evidence/ai-agent/safety-block/`: high-risk question raw SSE generated with `tools/ai_agent_evidence_capture.sh safety-test` plus `python3 tools/capture_ai_chat_device_evidence.py --scenario safety-block` output; include Android error terminal screenshot, UI tree, logcat, RunTrace `safetyResult.passed=false`, and proof that assistant streaming / stop affordance ended.
- `docs/acceptance-evidence/ai-agent/cancel-clear-stop/`: stop generation, clear chat and leave-collection captures with `tools/ai_agent_evidence_capture.sh cancel-test` and `python3 tools/capture_ai_chat_device_evidence.py --scenario stop|clear`; include cancel HTTP/SSE, OkHttp or log evidence for connection release, audit status and active-run cleanup.
- `docs/acceptance-evidence/ai-agent/combined-current/`: generated with `tools/ai_agent_combined_evidence_gate.py`, pairing the relevant interface package and Android package for `chat`, `safety-block`, `stop` and `clear`; any `partial-combined-*` status keeps the corresponding AGT-P0 row partial.
- `docs/acceptance-evidence/ai-agent/current-device-flow-7b716845/`: fresh unlocked-device screenshots / UI tree / logcat after commit `7b716845`.
- `docs/acceptance-evidence/ai-agent/device-ai-chat-current/`: generated by `tools/capture_ai_chat_device_evidence.py`, containing AI chat screenshot, UI tree, logcat, gfxinfo and `11-chat-evidence.json` / `12-conclusion.md`.
- `docs/acceptance-evidence/ai-agent/performance-current/`: AI run first event, first visible answer, first result block, tool, model, server notice, total duration, `tools/ai_agent_performance_evidence.py --android-gfxinfo <09-gfxinfo.txt>` frame stats and slow warning table.
- `docs/acceptance-evidence/performance/ai-agent-concurrency-current/`: repeated `/v2/agent/chat/stream` samples with concurrent runs, provider stream enabled, slow model / interrupted model scenarios and Android frame timing.
