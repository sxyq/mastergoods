# 历史会话验收说明

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 测试验收 |
| 当前状态 | Android 已完成（本地）；8220 无数据 Blocked |
| 适用端 | Agent |
| 依据源码 | `V2AgentConversationService.java`、Android `AgentChatViewModel` |
| 依据测试 | `testing/Agent/功能测试/TEST_PLAN.md`（Wave 1 history reload）、`AgentWorkbenchHistoryTest.kt` |
| 依据证据 | `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md`（历史分页恢复首个可见消息位置） |
| 最后核对 | 2026-08-20 |

## 一、验收范围

1. 会话列表加载（分页）。
2. 会话详情与消息历史恢复。
3. 历史消息分页加载（loadMoreMessages）。
4. 恢复后消息与结果块正确渲染。
5. 历史会话切换/删除。

## 二、验收标准

| 项 | 标准 |
|---|---|
| 列表 | 会话按 owner 分页返回 |
| 恢复 | 消息顺序正确，结果块按 part 顺序渲染 |
| 分页 | 恢复首个可见消息位置（Android 已确认） |
| 一致性 | 历史与实时消息模型一致 |

## 三、当前证据

- Android 历史分页恢复首个可见消息位置（8220 基线确认，本地验证）。
- 历史会话 UI 测试：`testing/.artifacts/2026-07-27-final-android/06-conversation-list.png`、`07-history-restored.png`（154 时期，historical-only）。

## 四、已知问题

- 历史消息恢复未恢复 RunTrace（#3）：`AgentMessageDto.toChatMessage()` 只映射 content/blocks/parts。

## 对应实现

- 后端代码：`V2AgentConversationService.java`
- Android 代码：`AgentChatViewModel`、`ConversationListPanel.kt`
- iOS 代码：`AgentViewModel.swift`
- Web 代码：`AgentPage.vue`
- Agent 代码：`V2AgentConversationService`

## 对应接口

- 接口路径：`/v2/agent/conversations`、`/v2/agent/conversations/{id}/messages`
- 请求模型：无
- 响应模型：`AgentConversationResponse`、`AgentMessageResponse`
- SSE 事件：无

## 对应测试

- 单元测试：`AgentWorkbenchHistoryTest.kt`
- 功能测试：`testing/Agent/功能测试/TEST_PLAN.md`（Wave 1）

## 当前限制

- 未完成内容：RunTrace 历史恢复（#3）
- Blocked 内容：8220 无会话数据
- Deferred 内容：无
- historical-only 内容：154 环境历史会话证据
