# 当前环境总览

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 运维 |
| 当前状态 | 已完成 |
| 适用端 | 后端 |
| 依据源码 | `application-prod.yml`、`Code/backend/Dockerfile` |
| 依据测试 | `testing/README.md` |
| 依据证据 | `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md` |
| 最后核对 | 2026-08-31 |

## 一、环境矩阵

| 项 | 当前值 | 历史值（154，historical-only） |
|---|---|---|
| 目标服务器 | `8.220.206.9` | `154.217.241.207`（已退役） |
| 公共入口 | `https://zhj-api.sxyq27.online/` | `https://sxyq27.online/zhj-api/`（410） |
| 容器 | `sxyq27-zhj-api` | - |
| 镜像 | `sxyq27-zhj-api:20260831T112015-C-worktree-owner-agent` | - |
| 回滚镜像 | `sxyq27-zhj-api:rollback-20260830T052508-admin-c2d9040c-dirty` | - |
| 应用端口 | `127.0.0.1:18080` | - |
| 数据库 | PostgreSQL 15 / Flyway V41 | - |
| Provider | glm-5.3-flash / oneapi / chat_completions（8220 运行时核对） | gpt-5.6-luna / responses（末期） |
| Agent 上下文配置上限 | 272000 tokens（源码默认；Provider 实际窗口需核对） | 旧版本窗口配置 |
| Android 默认地址 | 已迁移到 `https://zhj-api.sxyq27.online/` | 旧入口 410 |

## 二、当前数据库边界

以下为 2026-08-31 从 8220 PostgreSQL 读取的汇总计数，只用于环境核对，不包含业务明细：

| 范围 | 只读汇总 |
|---|---|
| 身份与组织 | `users=3`、`admin_accounts=1`、`stores=2`、`sessions=6` |
| Agent | `agent_conversations=0`、`agent_messages=0`、`agent_run_audits=0`、`agent_run_audit_events=0`、`agent_drafts=0`、`agent_context_checkpoints=0` |
| 业务 | `products=693`、`customers=83`、`suppliers=358`、`sale_orders=3846`、`purchase_orders=7`、`finance_records=2661`、`accounts=5`、`payments=904`、`inventory_snapshots=693`、`inventory_ledger=0`、`media_assets=0` |
| 管理员扩展 | `admin_audit_events=16`、`admin_scope_grants=0`、`admin_agent_configs=0`、`admin_export_jobs=0`、`admin_retention_policies=0` |

## 三、鉴权与入口

- 未鉴权访问公共入口当前返回 401（鉴权保护结果）。
- 旧入口 `https://sxyq27.online/zhj-api/` 返回 410（已退役）。
- 店主端入口为 `https://sxyq27.online/zhj/`；独立管理员 Web 入口为 `https://sxyq27.online/zhj-admin/`，前端 API 均直连 `https://zhj-api.sxyq27.online/`。
- 未鉴权访问 `GET /v2/admin/session` 当前返回 401；这只证明访问保护生效。

## 对应实现

- 后端代码：`application-prod.yml`
- Android 代码：`core/network/NetworkConfig.kt`（默认地址）
- iOS 代码：`Core/API/APIEndpoint.swift`
- Web 代码：`shared/api/config.ts`
- Agent 代码：不适用

## 对应接口

- 接口路径：`https://zhj-api.sxyq27.online/`
- 请求模型：无
- 响应模型：无
- SSE 事件：无

## 对应测试

- 基线：`testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md`；管理员发布记录：`testing/admin/20260830-production-deployment.md`
- 台账：`testing/current-8220-wave-ledger.csv`

## 当前限制

- 未完成内容：最新管理员 Web 构建的带权限页面复核、PostgreSQL 查询计划和 Android Agent 全链路实测
- Blocked 内容：Android 测试账号密码未取得，无法在 App 建立真实 session；管理员跨范围 HTTP、查询计划和 Agent 真实链路缺少获批测试范围
- Deferred 内容：iOS 本轮不处理；浏览器窄屏深度交互、多模态 Provider/App 结果
- historical-only 内容：154 环境全部内容
