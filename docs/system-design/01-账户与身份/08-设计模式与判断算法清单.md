# 01.8 — 账户模块设计模式与判断算法清单

> 状态：下一轮专题详细设计的工作清单。  
> 本文先登记范围，不在本次重复展开。

# 1. 下一轮要详细拆分的设计模式

1. Password Reset Strategy + Resolver；
2. Verification Strategy + Provider Registry + Adapter；
3. Password Policy Engine；
4. Login / Registration / Recovery Policy Pipeline；
5. Specification Pattern：CanRegister / CanLogin / CanReset / CanJoinMerchant；
6. Account / Invitation / Challenge / Recovery State Machine；
7. Command Pattern：Reset / Disable / ForceLogout / TransferOwner；
8. Idempotency Pattern；
9. Outbox + Retry Strategy；
10. Session Revocation Strategy；
11. Risk Rule Composite / Chain；
12. Identifier Resolver / Normalizer；
13. Audit Interceptor；
14. Transaction / Unit of Work。

# 2. 下一轮要详细分析的判断算法

1. STAFF 是否允许加入 Merchant；
2. 邀请码配额原子判断；
3. 登录允许 / Step-up / 拒绝；
4. 密码重置 Strategy 选择；
5. PHONE / EMAIL 验证方式可用性；
6. Risk Score 计算和阈值判定；
7. Session 是否有效；
8. 临时密码是否有效；
9. Identifier 唯一性与归属；
10. Password Policy 合规性；
11. Account Security State 状态迁移；
12. Reset / Registration 幂等判断；
13. Rate Limit 窗口算法；
14. Outbox 重试 / 退避算法。

下一轮应对每一项给出：

```text
输入
输出
规则
时序图
伪代码
复杂度
并发条件
索引需求
失败处理
Audit
```
