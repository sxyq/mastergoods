# 数据隔离审计

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 运维（问题审计） |
| 当前状态 | 已完成 |
| 适用端 | 后端 |
| 依据源码 | `CurrentOwnerService.java`、`infrastructure/repository/`、`SseStreamEmitter.queryWindowFor()`、`V7__owner_scope_foundation.sql`、`V24__owner_scoped_query_indexes.sql` |
| 依据测试 | `CurrentOwnerServiceTest.java`、`V2StoreControllerPermissionTest.java`、`V2SyncImportControllerTest.java` |
| 依据证据 | `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md` |
| 最后核对 | 2026-08-20 |

## 一、隔离机制核对

| 层次 | 机制 | 证据 |
|---|---|---|
| 数据库 | owner_user_id 字段 + V7/V24 索引 | 迁移文件 |
| 应用 | `CurrentOwnerService.requireCurrentOwnerUserId()` | 源码 |
| 查询 | Repository 按 owner 过滤（如 `findByRunIdAndOwnerUserId`） | 源码 |
| Agent 工具 | `query_window.owner_scope=current_owner` | `SseStreamEmitter.queryWindowFor()` |
| 权限 | `@RequireStorePermission` + `StoreAccessPolicy` | 源码 |

## 二、审计结论

1. 所有 Agent 表（conversations/messages/drafts/run_audits/tasks/notifications）与业务表均按 owner 隔离。
2. Agent 工具审计 `evidence.scope=current_owner`（`ToolAudit.evidenceSummary()`）。
3. 未发现绕过 owner 过滤的 Repository 查询证据（以源码检索为准）。
4. 交叉 owner 运行验证：8220 无多 owner 数据，运行级审计 Blocked。

## 三、待验证项

- 8220 多账号交叉访问的运行级验证（需多 owner 数据）。
- iOS/Web 本地缓存隔离验证（如存在本地缓存）。

## 对应实现

- 后端代码：`CurrentOwnerService.java`、`infrastructure/repository/`、`infrastructure/security/`
- Android 代码：`core/datastore/`
- iOS 代码：`Core/Auth/`
- Web 代码：`app/stores/session.ts`
- Agent 代码：`SseStreamEmitter.queryWindowFor()`

## 对应接口

- 接口路径：全部
- 请求模型：无
- 响应模型：无
- SSE 事件：工具事件 `query_window` / `evidence`

## 对应测试

- 单元测试：`CurrentOwnerServiceTest.java`、`V2StoreControllerPermissionTest.java`
- 功能测试：`testing/后端/功能测试/TEST_PLAN.md`（owner 场景）

## 当前限制

- 未完成内容：8220 运行级交叉隔离验证
- Blocked 内容：8220 无多 owner 数据
- Deferred 内容：无
- historical-only 内容：154 隔离行为
