# 问题解除条件

## 文档信息

| 字段 | 内容 |
|---|---|
| 文档类型 | 运维（问题审计） |
| 当前状态 | 已完成（解除条件清单） |
| 适用端 | 多端 |
| 依据源码 | `testing/已知问题与解除条件.md` |
| 依据测试 | `testing/current-8220-wave-ledger.csv` |
| 依据证据 | `testing/.artifacts/` |
| 最后核对 | 2026-08-20 |

## 一、解除条件清单（未解除问题）

| # | 问题 | 解除条件 |
|---|---|---|
| 1 | 流式断连误报 | 明确 `Socket closed` 根因并修复客户端 SSE / 代理 / 后端流式输出；真机复测留存截图与 logcat |
| 2 | 草稿列表无入口 | 增加可达入口或受支持启动方式直达 draft_list；补齐后验证草稿列表/确认/取消闭环 |
| 3 | 历史不恢复 RunTrace | 在真机运行中状态点开"运行过程"卡片稳定展开安全/计划/工具/审计细节；留存截图/录屏并回填台账 |
| 6 | 文本生图未闭环 | 明确提交后的网络请求与回写逻辑，修复客户端状态或后端返回路径；复测留存生成态/结果页/错误提示证据 |
| 7 | 极早取消 STREAM_ERROR | 明确 cancelRun 与 SSE emitter 完成/关闭时序；修复后 audit 稳定为 cancelled |
| 9 | sales-trend 500 | 修复部署端 500；同日期范围复测 summary 与 trend 均 200 且一致 |
| 10 | 首页顶部标题重叠 | 调整 edge-to-edge/insets 与首页标题布局；真机 + 小屏复测截图 |
| 11 | 快捷词被安全层拒绝误报断连 | 快捷词发送完整可判定只读问题，或安全层对低风险模糊请求返回澄清；Android 呈现服务端终态 |
| 12 | run 完成后停留旧会话 | 修复 run/conversation 与 UI message list 状态归属；收到 answer_completed/run_completed 后正常结束 SSE 并清除旧错误；录屏显示当前问题/回答/结果块/轨迹 |
| 16 | 只读请求被写入限流误判 | 写入频率只在 CREATE_ONLY 计划执行前计数；正确处理否定语义；补连续只读不消耗写配额测试 |
| 18 | 长历史滚动卡顿 | 定位长消息/结构化 block/列表复用热点并优化；同设备重复 3 轮 Perfetto/gfxinfo/meminfo 达阈值 |
| 19 | IME 顶部栏重叠 | 修正 WindowInsets/imePadding/顶栏协作；真机 + 小屏复测输入/滚动/关闭键盘/返回导航 |

## 二、已解除问题记录

| # | 问题 | 解除时间/证据 |
|---|---|---|
| 8 | 无 ADB 设备 | 2026-07-18 解除（`testing/.artifacts/2026-07-18-android-wave0/adb-devices-before-install.txt`）；当前 8220 真机重新 Blocked |
| 13 | 154 部署行为不一致（主要项） | 2026-07-26 重部署解除；154 为 historical-only |
| 14 | 规则摘要正式回答 | 2026-07-29 解除（`testing/.artifacts/2026-07-29-agent-real-model-no-template/`） |
| 15 | 工具参数 schema | 已修复（`AgentTool.parameterSchema`）；原生 function_call_output 续轮 8220 待验证 |
| 17 | 会话列表入口（子场景） | 2026-07-27 解除（`testing/.artifacts/2026-07-27-final-android/`） |

## 三、发布门禁解除前置

1. 8220 数据基线（session/store/Agent 数据）就绪。
2. Wave 1 真实数据 Agent 通过。
3. Android 真机可用。
4. 问题 #1、#7、#12 在 8220 复测通过。

## 对应实现

- 后端代码：`V2AgentAiService`、`SafetyGuard`、`SseStreamEmitter`
- Android 代码：`AgentSseClient`、`AgentChatViewModel`、`DashboardScreen.kt`
- iOS 代码：不涉及
- Web 代码：不涉及
- Agent 代码：`AnswerSynthesizer`

## 对应接口

- 接口路径：`/v2/agent/*`、`/v2/reports/*`
- 请求模型：无
- 响应模型：无
- SSE 事件：`error`、`run_completed`、`run_cancelled`

## 对应测试

- 证据：`testing/.artifacts/`
- 台账：`testing/current-8220-wave-ledger.csv`

## 当前限制

- 未完成内容：未解除问题见上表
- Blocked 内容：8220 数据链路、Android 真机
- Deferred 内容：#6（生图，多模态范围）
- historical-only 内容：154 环境解除证据
