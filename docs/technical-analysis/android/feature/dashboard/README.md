# Android feature/dashboard 模块分析

- 对应源码目录：`master-goods-android/feature/dashboard`
- 关键源码：
  - `DashboardScreen.kt`
  - `DashboardViewModel.kt`

## 模块定位

`feature/dashboard` 在新版中承担的是**owner 私有经营总览入口**。  
重点不是视觉卡片长什么样，而是首页应该能承载什么经营信息与导航分发：

- 当前 owner 的经营摘要
- 导入/同步健康状态
- 待处理业务入口
- AI 助手建议入口

## 状态图例

- `新版已做`
- `新版待做`
- `旧版存在新版未做`
- `新版需要去掉`
- `需重构`
- `待验证`

## 状态表

| 对象 | 状态 | 旧版情况 | 新版目标 | 当前实现 | 备注 |
|---|---|---|---|---|---|
| 首页首版经营看板 | 新版已做 | 旧版首页信息更偏本地账本概览 | 先支撑当前五栏首页 | 页面与 ViewModel 已存在，本轮继续补强为更贴近设计稿的总览/待办/资金/库存分组 | 已接入主壳 |
| 深度 KPI、库存/账户综合看板 | 旧版存在新版未做 | 旧版更厚的经营域可支持更细概览 | 新版 dashboard 要基于扩域后端增强 | 当前仍偏首版 KPI 和快捷入口 | 等后端扩域 |
| owner 同步/导入状态卡 | 新版待做 | 旧版无统一 owner | 首页感知 owner 初始化、导入与同步状态 | 当前 `DashboardViewModel` 仅聚合销售/财务/商品三类 `/v2` 数据，尚未接入 `SyncV2Repository` | 新版首页重要职责，但这一子链尚未落地 |

## 本轮补充说明

- 本轮 UI 调整集中在 `DashboardScreen.kt`，主要是复用既有 design system 组件提升首页贴合度，并补清晰的本地聚合/待 B11 真机验证提示。
- 后续又继续收口了两类“看起来像真状态机、其实只是本地聚合”的表达：把“待审核销售单”降级为销售单概览提醒，并让低库存列表右侧主指标改成缺口量而不是售价。
- 首页 `FilterChipRow` 当前明确承担“看板关注视角切换”，不再暗示自己是完整业务筛选；`总览/待办提醒/库存预警/回款重点` 四种视角只控制对应业务块显示，快捷入口与总览卡片继续保留。
- 当前仍未把 dashboard 的同步/导入健康状态接入首页，不应把这次修改解读为 B11 首页联调完成。

## UI 统一约束

- 本模块后续新增业务必须继续复用当前设计图对应的页面母版，不允许因为领域变厚就切换成另一套视觉语言。
- 页面结构优先落入既有模式：列表页、详情页、编辑页、报表页、AI 页、设置页。
- 视觉基线固定为：浅蓝渐变背景、玻璃卡片、蓝色主按钮、白色次按钮、统一状态标签、五栏主壳。
- 如需新增 UI 组件，先沉淀到 `core/designsystem`，再由本模块复用；不允许长期保留 feature 私有样式组件。
- 验收时同时对照：`docs/design-mockups/01.png ~ 08.png`、`master-goods-android/UI-DESIGN-SPEC.md`、`docs/technical-analysis/android/core/designsystem/README.md`。
