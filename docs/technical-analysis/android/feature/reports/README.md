# Android feature/reports 模块分析

- 对应源码目录：`master-goods-android/feature/reports`
- 关键源码：
  - `ReportScreen.kt`
  - `ReportViewModel.kt`

## 模块定位

`feature/reports` 在新版里承接的是 owner 私有经营报表前台。  
重点不在图表配色，而在报表域能否覆盖：

- 销售/采购汇总
- 库存账本/月统计
- 账户与现金流
- AI 辅助分析入口

## 状态图例

- `新版已做`
- `新版待做`
- `旧版存在新版未做`
- `新版需要去掉`
- `需重构`
- `待验证`

## 状态表

| 对象 | 状态 | 旧版情况 | 新版目标 | 当前实现 | 备注 |
|---|---|---|---|---|---|
| 报表首版页面 | 新版已做 | 旧版统计项可更厚 | 先承载当前经营报表展示 | 页面与 ViewModel 已存在，本轮继续补强为更贴近设计稿的总览/库存/回款/风险结构 | 已接入主壳 |
| 库存/月结/账户类报表 UI | 旧版存在新版未做 | 旧版库存和财务统计更丰富 | 新版报表页要覆盖扩域后端 | 当前主要是首版统计卡片 | 依赖后端扩域 |
| owner 私有报表视图 | 待验证 | 旧版无统一 owner | 所有报表只展示当前 owner 数据 | 已切到 V2 Repository 客户端聚合（待验证） | 本模块已使用 V2 Repository 替代 V1 Repository |

## 本轮补充说明

- 本轮 UI 调整集中在 `ReportScreen.kt`，主要补总览卡、搜索/筛选、重点回款客户和风险洞察列表，尽量用现有 `core/designsystem` 组件收口视觉语言。
- 后续又继续收紧“诚实态”边界：时间 Tab 目前只明确作用于销售单相关汇总，账户余额/库存/成本类卡片改为清楚标注“当前快照”或“当前月估算”，避免把整页误读成真实多周期报表。
- 原先本地拼出来的“销售趋势”折线已降级成待联调说明卡，直到有稳定趋势序列来源前，不再用伪造曲线冒充真实趋势图。
- 报表页 `FilterChipRow` 当前也明确承担“报表关注视角切换”，不再伪装成服务端业务筛选。
- 当前报表仍以客户端聚合为主，真机截图、117 现场 smoke、性能与发布环境证据仍属于 B11 待验证项。

## UI 统一约束

- 本模块后续新增业务必须继续复用当前设计图对应的页面母版，不允许因为领域变厚就切换成另一套视觉语言。
- 页面结构优先落入既有模式：列表页、详情页、编辑页、报表页、AI 页、设置页。
- 视觉基线固定为：浅蓝渐变背景、玻璃卡片、蓝色主按钮、白色次按钮、统一状态标签、五栏主壳。
- 如需新增 UI 组件，先沉淀到 `core/designsystem`，再由本模块复用；不允许长期保留 feature 私有样式组件。
- 验收时当前优先对照：`docs/spec/42-android-liquid-glass-ui-refactor-plan.md`、Stitch 导出清单、`master-goods-android/UI-DESIGN-SPEC.md`、`docs/technical-analysis/android/core/designsystem/README.md`；`docs/design-mockups/` 仅作历史参考。
