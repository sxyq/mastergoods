# feature/settings 当前边界

设置页当前已接入账号信息、受控服务端地址、手动同步、导入任务观察、店员账号管理和退出登录。

## 尚未完成

- 缓存统计和独立缓存清理入口。
- 完整 store/member 角色绑定体系，当前仅为真实 `admin users` 管理。
- 设置页真机验证和同步失败、权限不足、空数据状态验收。

## 验收约束

- 未接入的缓存能力必须显示诚实说明，不得展示可执行的假入口。
- 同步、导入和权限状态必须来源于真实 API 返回。

## 验收入口

- 源码：`feature/settings/src/main/`
- 测试台账：`testing/安卓/`、`testing/后端/`
