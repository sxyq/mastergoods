# 已知问题与解除条件

更新时间：2026-07-19

这份文档用于集中记录本轮测试中发现、但尚未由代码修复解决的问题，以及对应的解除条件。它只记录真实执行中遇到的现象，不记录推测。

## 1. Android Agent 流式断连

- 现象：真机 `Agent` 新流式发送后，UI 提示 `AI 流式连接中断，请检查网络后重试`。
- 证据：
  - `testing/.artifacts/android-agent-stream-error.png`
  - `testing/.artifacts/android-chat-history-summary.txt`
  - `testing/.artifacts/2026-07-06-real-query-chip2.png`
  - `testing/.artifacts/2026-07-06-real-query-final.png`
  - `testing/.artifacts/2026-07-06-android-cancel/assistant-chat.png`
  - `testing/.artifacts/2026-07-06-android-cancel/current-during-stream.png`
  - `testing/.artifacts/2026-07-06-android-cancel/logcat-after-stop.txt`
  - `testing/.artifacts/2026-07-06-live-app-relaunch.png`
  - `testing/.artifacts/2026-07-06-after-assistant-tab-live-2.png`
  - `testing/.artifacts/2026-07-06-after-start-real-chat.png`
  - `testing/.artifacts/2026-07-06-after-real-query-chip-live.png`
  - `testing/.artifacts/2026-07-06-after-real-query-final-live.png`
  - `testing/.artifacts/2026-07-06-after-real-query-logcat.txt`
- 关联台账：
  - `testing/安卓/功能测试/functional_feature_matrix.csv`
  - `testing/安卓/测试分类总台账.csv`
- 当前结论：Blocked / 失败，不得记为通过。
- 解除条件：
  - 明确 `Socket closed` 的根因并修复客户端 SSE 处理、代理转发或后端流式输出中的一处真实问题。
  - 修复后需再次真机复测并留存截图与 logcat。

## 2. Android 草稿列表 UI 无可达入口

- 现象：`DraftListScreen` 路由已注册，但当前真机 UI 里没有可见入口，无法从工作台进入草稿列表。
- 证据：
  - `testing/.artifacts/2026-07-05-android-draft-route.txt`
  - `testing/.artifacts/zhihuiji-ui-assistant3.xml`
  - `testing/.artifacts/2026-07-18-android-wave0/agent-entry-ui-2.xml`
  - `testing/.artifacts/2026-07-18-agent-android-wave1/chat-entry-ui-2.xml`
  - `testing/.artifacts/2026-07-18-agent-android-wave1/tasks-notifications-ui.xml`
  - `Code/frontend/android/app/src/main/java/com/zhihuiji/app/navigation/MainNavGraph.kt`
  - `Code/frontend/android/feature/agent/src/main/java/com/zhihuiji/feature/agent/AgentWorkbenchScreen.kt`
- 关联台账：
  - `testing/安卓/功能测试/functional_feature_matrix.csv`
  - `testing/安卓/测试分类总台账.csv`
- 当前结论：Blocked / 部分完成。
- 解除条件：
  - 增加可达入口，或补一个受支持的启动方式直达 `draft_list`。
  - 入口补齐后需重新验证草稿列表、确认、取消闭环。

## 3. Android Agent 审计/轨迹入口仅在运行中状态可见

- 现象：真机在 `真实查询` 运行中曾可见轨迹状态；2026-07-18 复测既有 conversation `57` 时，历史正文/KPI/表格均恢复，但完成态没有运行过程或审计入口。源码 `AgentMessageDto.toChatMessage()` 只映射 content/blocks/parts，未恢复 `RunTrace`。
- 证据：
  - `testing/.artifacts/2026-07-06-real-query-chip2.png`
  - `testing/.artifacts/2026-07-06-real-query-final.png`
  - `testing/.artifacts/2026-07-06-audit-attempt.png`
  - `testing/.artifacts/2026-07-18-agent-android-wave1/history-ui.xml`
  - `testing/.artifacts/2026-07-18-agent-android-wave1/history.png`
  - `Code/frontend/android/feature/agent/src/main/java/com/zhihuiji/feature/agent/AgentChatViewModel.kt`
- 关联台账：
  - `testing/安卓/功能测试/functional_feature_matrix.csv`
  - `testing/安卓/测试分类总台账.csv`
- 当前结论：部分可见，未闭环。
- 解除条件：
  - 在真机运行中状态下再次点开 `运行过程` 卡片并稳定展开安全/计划/工具/审计细节。
  - 展开后留存截图或录屏，并同步回填台账。

## 4. Android ADB 中文输入限制

- 现象：`adb shell input text` 直接输入中文时，出现 `NullPointerException`。
- 证据：
  - `testing/.artifacts/android-chinese-input-test-summary.txt`
- 影响：只能通过已有历史、脚本或其他输入方式完成中文场景验证。
- 解除条件：
  - 改用支持中文输入的注入方式，或通过自动化脚本/剪贴板方案完成输入。

## 5. 草稿确认的最小夹具要求

- 现象：`create_sale_order` 草稿确认时，如果 `items` 为空，会直接失败并提示 `订单明细不能为空`。
- 证据：
  - `testing/.artifacts/backend-draft-confirm-cancel-summary.txt`
  - `testing/.artifacts/backend-draft-confirm-success-summary.txt`
- 影响：测试必须先准备最小商品夹具，再做 confirm。
- 解除条件：
  - 这不是产品缺陷，不需要修复；只需在测试执行时遵守最小夹具策略。

## 6. Android 文本生图提交后未见生成态或结果回写

- 现象：真机 `Agent` 页面可打开“文本生图”弹层，提示词输入框可编辑，`开始生成` 也可点亮；但点击提交后，界面未进入可见生成态，也未看到结果页或明确失败提示，最后回到聊天页。
- 证据：
  - `testing/.artifacts/ui_modal_filled.xml`
  - `testing/.artifacts/shot_modal_filled.png`
  - `testing/.artifacts/ui_after_text_generate_submit.xml`
  - `testing/.artifacts/shot_after_text_generate_submit.png`
- 关联台账：
  - `testing/安卓/功能测试/functional_feature_matrix.csv`
  - `testing/Agent/功能测试/functional_feature_matrix.csv`
- 当前结论：Blocked / 未闭环，不得记为“完整通过”。
- 解除条件：
  - 明确按钮提交后的实际网络请求与回写逻辑，修复客户端状态切换或后端返回路径中的真实问题。
  - 修复后需重新提交文本生图并留存生成态、结果页或错误提示的真机证据。

## 7. 后端 Agent 取消链路在极早取消时出现流式收尾失败

- 现象：对 `POST /v2/agent/chat/stream` 发起后，在 `run_started` 后立即取消，SSE 记录了 `run_cancelled`，但对应 audit 最终落成 `failed`，错误为 `STREAM_ERROR`，消息为 `ResponseBodyEmitter has already completed`。
- 证据：
  - `testing/.artifacts/2026-07-06-wave1-backend-stream/audit.json`
  - `testing/.artifacts/2026-07-06-wave1-backend-stream/meta.json`
  - `testing/.artifacts/2026-07-06-wave1-backend-stream/stream_raw.txt`
- 关联台账：
  - `testing/后端/功能测试/functional_feature_matrix.csv`
  - `testing/Agent/功能测试/functional_feature_matrix.csv`
- 当前结论：失败，不得记为通过。
- 解除条件：
  - 明确 `cancelRun` 与 SSE emitter 的完成/关闭时序，避免取消时进入已完成 emitter 的异常分支。
  - 修复后需重新执行流式 + 取消链路，并在 audit 中看到稳定 `cancelled` 结果。

## 8. Android 真机当前无 ADB 设备（已解除）

- 现象：本轮复核时 `adb devices -l` 为空，`d715a3a4` 未出现在设备列表中，无法继续真机安装状态、包名可达性与页面联调验证。
- 证据：
  - `adb devices -l`
  - `/Users/sunyiyang/Library/Android/sdk/platform-tools/adb devices -l`
- 解除证据（2026-07-18）：
  - `testing/.artifacts/2026-07-18-android-wave0/adb-devices-before-install.txt`
  - `testing/.artifacts/2026-07-18-android-wave0/adb-install-r.txt`
  - `testing/.artifacts/2026-07-18-android-wave0/package-after-install.txt`
  - `testing/.artifacts/2026-07-18-android-wave0/cold-start.txt`
- 当前结论：已解除。`d715a3a4` 在线，当前源码 APK 已覆盖安装成功，`com.zhihuiji.app/.MainActivity` 冷启动成功。
- 历史影响：2026-07-06 当时阻塞 Android Wave 0 / Wave 1 真机联调；历史 Blocked 记录保留，不回写成通过。
- 解除条件：
  - 重新连接目标真机并让设备出现在 ADB 列表中。
  - 如确实发生了卸载或包丢失，需要先重新安装 `com.zhihuiji.app`，再继续登录 / session 恢复 / Agent 主链路验证。

## 9. Android 首页销售趋势接口返回 500

- 现象：2026-07-18 真机冷启动后，同一首页范围内 `/v2/reports/sales-summary` 返回 200，UI 显示销售额 `¥37.00` 和 `2` 张真实订单；但 `/v2/reports/sales-trend` 返回 500，趋势卡显示总计 `¥0.00`。
- 证据：
  - `testing/.artifacts/2026-07-18-android-wave0/launch-logcat-redacted.txt`
  - `testing/.artifacts/2026-07-18-android-wave0/launch.png`
  - `testing/.artifacts/2026-07-18-android-wave0/launch-ui.xml`
- 关联台账：
  - `testing/安卓/功能测试/functional_feature_matrix.csv`
  - `testing/安卓/测试分类总台账.csv`
- 当前结论：失败，不得将首页报表/趋势能力记为通过。
- 解除条件：
  - 在不改变当前账户数据口径的前提下修复部署端 `sales-trend` 500。
  - 修复后以同一日期范围复测，要求 summary 与 trend 都返回 200，且趋势汇总与真实订单数据一致。

## 10. Android 首页顶部标题区域重叠

- 现象：2026-07-18 真机 `1080x2400 / 420dpi` 冷启动首页后，顶部 `智慧记` 与页面标题 `近1年经营概览` 明显重叠，层级不可清晰阅读。
- 证据：
  - `testing/.artifacts/2026-07-18-android-wave0/launch.png`
  - `testing/.artifacts/2026-07-18-android-wave0/launch-ui.xml`
  - `Code/frontend/android/feature/dashboard/src/main/java/com/zhihuiji/feature/dashboard/DashboardScreen.kt`
- 关联台账：
  - `testing/安卓/功能测试/functional_feature_matrix.csv`
  - `testing/安卓/测试分类总台账.csv`
- 当前结论：失败；首页可达性通过不覆盖该布局失败。
- 解除条件：
  - 调整 edge-to-edge/insets 与首页标题布局，使系统状态栏、App 顶栏、页面标题各自占据清晰区域。
  - 修复后需在当前真机分辨率重新截图，并补至少一个小屏配置复测。

## 11. “真实查询”快捷词被安全层拒绝且 Android 显示为网络断连

- 现象：Android Agent 页面提供“真实查询”快捷词，但发送后服务端将其判为含义不明确并在业务 LLM/工具规划前阻断；服务端已保存 blocked 回答，Android 却显示“AI 流式连接中断，请检查网络后重试”。
- 服务端事实：
  - conversation `58`
  - run `46a6eab9-ffd6-4fd6-818c-d2e6a4f573f5`
  - `status=blocked`
  - `llm_status=not_requested`
  - `plan_source=safety`
  - `tool_count=0`
- 证据：
  - `testing/.artifacts/2026-07-18-agent-android-wave2/real-query.mp4`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/real-query-t8-ui.xml`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/run-46a6-audit.json`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/conversation-58-messages.json`
- 当前结论：失败。服务端 blocked 状态不等于测试 Blocked，本项已经真实执行且不符合快捷入口预期。
- 解除条件：
  - 快捷词发送完整、可判定的只读问题，或安全层对低风险模糊请求返回澄清提示而不是高风险拒绝。
  - Android 必须呈现服务端终态，不得把正常 blocked 完成误报为网络断连。

## 12. 后端 run 完成后 Android 仍停留旧会话并显示断连错误

- 现象：明确只读查询已在服务端完整调用模型与 3 个真实工具并落库，但 45 秒真机录屏末段仍显示旧 conversation `57` 的结果块与断连错误，未展示本次 conversation `59` 的问题或正式回答。
- 服务端事实：
  - conversation `59`
  - messages `117` / `118`
  - run `a1097a96-334c-4a9b-9434-d3b662a92b55`
  - `status=completed`
  - `mode=tool_query_llm_streamed`
  - `plan_source=native_tool_use`
  - `tool_count=3`
  - `event_count=35`
- 真实工具结果：商品 `3` 个、商品库存合计 `35`、库存快照 `2` 条且总值 `¥127.00`、低库存 `0` 个。
- 证据：
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/run-a109-audit.json`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/conversation-59-messages.json`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/agent-explicit-readonly.mp4`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/final-segment.mov.png`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/logcat-redacted.txt`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/02-stream-raw.sse`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/03-audit.json`
  - `testing/.artifacts/2026-07-19-android-wave2-performance/19-single-query2-logcat.txt`
- 2026-07-19 补充复现：run `2052a7da-17f0-4156-8a72-e8c0ebbd6834` 已发送 `answer_completed` 与 `run_completed`，但 curl 仍以错误 18 结束，Android 再次未展示当前回答并报告流中断。
- 当前结论：Android 失败；后端 stream / audit 与多工具查询分别通过。不得用后端通过覆盖客户端失败。
- 解除条件：
  - 修复当前 run/conversation 与 UI message list 的状态归属和完成态切换。
  - 收到 `answer_completed` / `run_completed` 后正常结束 SSE 消费并清除旧错误，不等待异常 EOF。
  - 复测时录屏必须显示当前问题、当前正式回答、相关结果块和正确工具轨迹。

## 13. 154 历史部署行为与当前工作树不一致（主要项已于 2026-07-26 解除）

> 历史执行快照：本节中的 154 配置、容器和运行结果只用于说明退役前状态。
> 当前 8220 运行状态以 `testing/.artifacts/2026-08-18-8220-current-baseline/current-8220-baseline.md` 为准。

- 现象：conversation `59` 的线上 assistant message 仍含 `evidence_card`；当前工作树的 `V2AgentAiServiceTest` 明确断言该默认块不存在。线上 audit 只记录 `llm_status` 与 `plan_source`，未记录 provider/model 名称。
- 配置现状：
  - `application-prod.yml` 默认 `AGENT_LLM_ENABLED=false`、模型 `deepseek-v4-flash`。
  - `deploy/154/docker-compose.yml` 使用同样默认值，允许环境变量覆盖。
  - 历史容器实际为 `AGENT_LLM_ENABLED=true`、`AGENT_LLM_MODEL=gpt-5.4-mini`；该历史状态已通过 2026-07-26 重部署替换为 `gpt-5.6-luna`，`AGENT_LLM_WIRE_API=responses`。
  - 当前 run audit 仍不记录 provider/model，仓库与 154 的行为也不一致。
  - 当前业务 Agent 已使用 `gpt-5.6-luna`；真实回归证明模型可选择业务只读工具和 `result_visualization`。
- 证据：
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/conversation-59-messages.json`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/run-a109-audit.json`
  - `testing/.artifacts/2026-07-18-agent-android-wave2/explicit-readonly/llm-tool-sse-investigation.md`
  - `testing/.artifacts/2026-07-19-deployment-config-audit/154-backend-inspect-sanitized.txt`
  - `testing/.artifacts/2026-07-19-deployment-config-audit/154-version-inspect-sanitized.txt`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/gpt-5.6-luna-readonly-investigation.md`
  - `Code/backend/src/test/java/com/zhihuiji/backend/application/service/v2/V2AgentAiServiceTest.java`
- 历史结论：154 退役前曾切换为 `gpt-5.6-luna` 并完成服务端重部署；该环境下普通回答无结构化块，图表问题由模型主动选择工具后返回真实图表。
- 当前结论：154 不属于当前运行环境；当前 Provider 状态和 8220 结果以当前基线文档为准。标准 `function_call_output` 续轮不能直接沿用 154 探针结果，需在 8220 上独立验证。
- 解除条件：
  - 查明 provider 接受的 `function_call_output` 续轮格式，并在隔离配置完成 luna 原生续轮闭环。
  - 将 provider/model 以非敏感字段写入 run audit，并补齐最终 LLM 回答而非事实摘要回退。
  - 已完成当前工作树构建部署与真实回归；默认 `evidence_card` 和默认图表均未出现。

## 14. 正式回答曾由规则摘要生成（已于 2026-07-29 解除）

- 历史现象：旧工作树的 `AnswerSynthesizer.buildFinalAnswer()` 与 `buildFinalAnswerForStream()` 直接调用 `synthesizeAnswer()` 拼接工具事实，并返回 `tool_query_rule_summary`；该正式回答路径不会再次调用 LLM。
- 当前实现：`AnswerSynthesizer` 先尝试 provider 原生工具结果续轮，失败时仍通过 `createJsonMessage` 或 `streamTextMessage` 请求同一真实模型；模型未配置、返回空或事实校验失败时返回空正文和错误状态，不生成规则摘要或模拟回答。
- 边界：历史 `tool_query_rule_summary` 证据属于旧源码/旧部署，不能作为当前实现行为；历史文件仍保留用于审计追溯。
- 证据：
  - `Code/backend/src/main/java/com/zhihuiji/backend/application/service/v2/agent/component/AnswerSynthesizer.java`
  - `testing/.artifacts/2026-07-29-agent-real-model-no-template/backend-agent-no-template-test.txt`
  - `testing/.artifacts/2026-07-29-agent-real-model-no-template/2026-07-29-rerun-record.txt`
  - `testing/.artifacts/2026-07-28-p1-verify/chart-nonstream.json`
- 当前结论：已解除。后端定向测试和真实回归证明正式回答走模型路径；历史固定规则摘要只作为旧版本证据保留。

## 15. 工具参数 schema 已修复；provider 原生多轮回灌仍受限

- 历史现象：run `2052...` 已正确调用商品和库存工具，但后续又调用缺少 `report_type` 的 `report_query`，并返回不相关的“支持的报表类型”表格。
- 根因证据：
  - 旧版本 `AgentTool.parameterSchema()` 默认返回 `null`。
  - 旧版本 `ReportQueryTool` 读取 `report_type` / `period`，但没有完整参数 schema。
  - 旧版本后续迭代把工具摘要拼进普通文本，没有实现 Responses 原生 `function_call_output` 回灌。
  - 旧版本 `ToolSupport` 与 `V2AgentAiService` 都可能发送 `tool_completed`，事件所有权不唯一。
- 证据：
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/03-audit.json`
  - `Code/backend/src/main/java/com/zhihuiji/backend/application/service/v2/agent/tool/AgentTool.java`
  - `Code/backend/src/main/java/com/zhihuiji/backend/application/service/v2/agent/tool/readonly/ReportQueryTool.java`
  - `Code/backend/src/main/java/com/zhihuiji/backend/application/service/v2/agent/component/ToolPlanner.java`
- 当前结论：schema、工具相关性和 `tool_completed` 重复发送问题已解除；当前 provider 原生 `function_call_output` 续轮探针仍返回 HTTP 400。服务端已保留真实模型重请求路径，不使用规则回答或模拟结果替代。
- 解除条件：
  - 由 provider 明确并验证可接受的 `function_call_output` 续轮格式，并在隔离环境完成 Luna 原生续轮闭环。
  - 继续保留 schema 合约、错误参数拒绝和多轮调用测试；该 provider 限制不应通过前端固定回答掩盖。

## 16. 只读长历史请求被写入频率限制误拦截

- 现象：30 轮只读 LLM 工具测试前 15 轮完成，第 16 轮返回“10 分钟内写入操作超过 20 条限制”。所有成功轮次只调用 `product_catalog_lookup`，没有业务写入。
- 根因：测试文本包含 `Do not create or modify data`；`SafetyGuard.evaluateWriteSafety()` 只检查是否包含 `create` 等关键词，不理解否定语义，仍将请求计入写入窗口。
- 证据：
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/summary-final.txt`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/real-readonly/timing.tsv`
  - `Code/backend/src/main/java/com/zhihuiji/backend/application/service/v2/agent/component/SafetyGuard.java`
- 当前结论：限流行为失败；完整 30 轮性能场景保持 Blocked，不能把前 15 轮写成整体通过。
- 解除条件：
  - 写入频率只在实际 `CREATE_ONLY` 计划执行前计数。
  - 正确处理“不要创建/只读”等否定语义，并补连续只读请求不消耗写配额的测试。

## 17. Android Agent 会话列表入口（2026-07-27 子场景已解除）

- 历史现象：旧 debug APK 曾只能通过启动 extra 加载 conversation `64` 的 32 条消息，正常 Agent 页面没有历史会话/抽屉入口。
- 历史证据仍保留：
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/12-conversation64.xml`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/13-conversation64.png`
- 2026-07-27 当前真机复核：从正常 Agent 空态进入聊天页，打开 `会话列表`，选择已有真实会话并恢复 `How many products are there` / `There are 3 products.`；无 crash/ANR。
- 当前结论：正常 UI 会话列表与历史恢复子场景通过；`AND-FT-11` / `AG-FT-AN-02` 中更早的流式归属、IME 和长历史失败继续独立保留，不因本子场景通过而覆盖。
- 当前证据：
  - `testing/.artifacts/2026-07-27-final-android/05-chat-from-empty.png`
  - `testing/.artifacts/2026-07-27-final-android/06-conversation-list.png`
  - `testing/.artifacts/2026-07-27-final-android/07-history-restored.png`
  - `testing/.artifacts/2026-07-27-final-android/07-history-restored.xml`

## 18. Android 32 消息长历史滚动卡顿且内存增长

- 现象：1038 帧中 174 帧卡顿，jank `16.76%`，p95 `77 ms`，p99 `129 ms`；滚动阶段 PSS 从 `219171 KB` 增至 `251859 KB`，增长 `32688 KB`。无崩溃或 ANR，但不能覆盖性能失败。
- 证据：
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/22-agent64-scroll.pftrace`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/23-gfxinfo-scroll-after.txt`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/18-meminfo-scroll-before.txt`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/24-meminfo-scroll-after.txt`
- 当前结论：失败；单次 PSS 增长是回归线索，不单独宣称已经证明内存泄漏。
- 解除条件：
  - 定位长消息、结构化 block 与列表复用的重组/分配热点并优化。
  - 同设备至少重复 3 轮 Perfetto/gfxinfo/meminfo，达到约定 jank 和内存阈值。

## 19. Android Agent 打开 IME 后顶部栏零尺寸并与状态栏重叠

- 现象：输入框打开 IME 后仍位于键盘上方，但顶部栏节点变为零尺寸，正文上移并与系统状态栏重叠。窗口可用内容高度记录为 `1687 px`。
- 证据：
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/28-keyboard-open.xml`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/29-keyboard-open.png`
  - `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/30-window-keyboard-open.txt`
- 当前结论：失败；输入框可见不代表完整页面 inset 适配通过。
- 解除条件：
  - 修正 `WindowInsets` / `imePadding` / 顶栏布局协作，保证 IME 开关前后顶部栏尺寸稳定。
  - 当前真机和至少一个小屏配置分别复测输入、滚动、关闭键盘和返回导航。
