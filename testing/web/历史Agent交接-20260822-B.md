# 历史 Agent 任务交接（2026-08-22，Agent B）

## 审查范围与结论

本轮只审查历史 Agent 任务材料，没有继续做 Web 源码扫描，也没有创建、fork、handoff 或修改线程。运行时未提供历史任务 `019fc464-d38e-7d02-bd13-6e27a80cb6c1` 的只读 thread reader，因此该线程没有被读取；结论均来自仓库内台账、报告和原始 artifacts。

已读取的主材料：

- `testing/README.md`
- `testing/FINAL-REPORT-20260820-8220.md`
- `testing/current-8220-wave-ledger.csv`
- `testing/Agent/功能测试/TEST_PLAN.md`
- `testing/Agent/功能测试/functional_feature_matrix.csv`
- `testing/Agent/功能测试/live_execution_ledger.csv`
- `testing/.artifacts/2026-08-20-8220-agent-retest/`
- 2026-07-19、2026-07-27、2026-07-31、2026-08-01、2026-08-02、2026-08-03、2026-08-05 的历史 Agent artifacts

当前可用于 C 重测的运行目标是 `8.220.206.9` 的 8220 backend，历史 `154.217.241.207` 已退役。8220 证据中的账号范围为 `owner_user_id=2`、`store_id=1`；当前只有一个有效 store membership。所有旧 154、旧 API 地址、旧 Android 设备和旧 provider 记录都只能作为问题复现输入或对照，不能并入当前通过率。

## 状态总览

| 范围 | 状态 | 已确认记录 | 原始证据 | 当前适用性 |
|---|---|---|---|---|
| 8220 Agent 全量 | Passed / Failed | 60 个真实 case：50 Passed、10 Failed、0 Blocked；失败都曾返回 HTTP 200 | `testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/full-tools-evidence/summary.json`、`case-status.tsv` | 8220 的最近基线；C 需重新执行 10 个失败 case |
| 8220 历史恢复 | Failed | 会话列表为空，workbench 和 run-traces 有会话；显式 `conversation_id` chat 返回 HTTP 400 | `testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/case-product-catalog.txt`、`case-product-catalog-retry.txt`、`auth-history/wave1-readonly-api.txt` | 直接影响当前 Agent/Android/Web 历史恢复，必须重测 |
| 8220 跨 owner | Blocked | 第二个用户没有有效门店和 session，未形成双 owner 实测 | `testing/.artifacts/2026-08-20-8220-agent-retest/auth-history/wave1-readonly-api.txt`、`testing/FINAL-REPORT-20260820-8220.md` | 当前仍 Blocked；C 需先具备第二 owner 的有效 store/session |
| 8220 SSE 完成 | Passed | 10/10 并发样本 HTTP 200、run_started、run_completed、回答和临时会话删除均完成 | `testing/.artifacts/2026-08-20-8220-agent-retest/performance/mg-8220-sse-concurrency-20260820/summary.txt`、`results.tsv` | 仅为 2026-08-20 样本；C 需在当前目标重测 |
| 8220 SSE cancel | Passed | run_started -> run_cancelled；cancel HTTP 200；审计终态 cancelled；删除 HTTP 200 | `testing/.artifacts/2026-08-20-8220-agent-retest/stream-cancel/mg-8220-sse-retest-20260820/` | 服务器样本已通过，Web/客户端终止链路和当前回归仍需 C 复测 |
| 8220 审计 | Passed / 待复测 | 取消样本 `audit_lossy=false`，事件数与 emitted_event_count 均为 2；全量 case 保留 audit_id | `stream-cancel/.../audit.json`、各 case JSON | C 需针对失败 case、完成流和取消流逐条 GET audit，不能只看 chat 返回体 |
| 8220 并发与延迟 | Passed（观察值） | 10 路 SSE：P50 17.574s、P95 19.143s；30 轮非流式：P50 18.245s、P95 25.414s | `performance/mg-8220-sse-concurrency-20260820/`、`performance/nonstream-30-summary.txt` | 没有已批准 SLA；只能记录分布，不能据此宣称性能达标 |
| 8220 会话/草稿清理 | Passed / 待复测 | 60 个全量 case 的临时会话和草稿计数恢复；失败 case 没有留下草稿 | `readonly-agent/full-tools-evidence/cases/*.json`、`summary.json` | C 需使用真实草稿创建链路验证创建后删除和失败后清理 |
| 8220 媒体清理 | Failed（自动清理风险） | 媒体删除曾出现文件残留，之后按精确路径人工清理，数据库和文件数才归零 | `testing/FINAL-REPORT-20260820-8220.md`、`testing/.artifacts/2026-08-20-8220-agent-retest/database/media-api-case.txt`、`database/final-state.txt` | C 必须重新验证删除后的文件系统状态，不能用人工清理后的零计数代替 |
| 8220 多模态 | Deferred | 生图、图片输入和图片结果展示等待 provider/设计资料 | `testing/FINAL-REPORT-20260820-8220.md` 第 8 节 | 当前不进入普通 Agent 通过率；资料具备后单独执行 |
| 154 owner 隔离 | Passed（旧环境） | owner 8 查询得到 0 个商品，读取 owner 7 商品返回 422；旧同步样本清理完成 | `testing/.artifacts/2026-08-03-server-sync-scope/remote-sync-scope.jsonl`、`remote-sync-scope.raw.tsv` | 只作跨 owner 输入参考；不能替代 8220 双 owner 证据 |
| 154 SSE cancel/草稿 | Passed（旧环境） | DeepSeek 旧 runtime 有 cancel、draft cancel/confirm/delete 样本 | `testing/.artifacts/2026-08-02-server-agent-eval/wave1-deepseek-functional-live-20260802T195600+0800/` | 旧 host、旧数据库和旧 provider，全部过期；C 必须重测 8220 |
| 154 soak | Failed（旧环境） | 12 个自然请求中 10 个完成，2 个在 110s 超时 | `testing/.artifacts/2026-08-03-production-agent-recheck-after-current-deploy/soak-20260803-approved-final-v2/` | 作为稳定性风险输入，不能合并到当前样本 |
| 154 同店多成员 | Blocked（旧环境） | 每个 live store 只有一个 membership，未执行双成员并发 | `testing/Agent/功能测试/live_execution_ledger.csv` 中 `AG-FT-W1-20260803-SAME-STORE-MULTI-MEMBER`、`testing/.artifacts/2026-08-03-server-sync-scope/` | 需要第二有效成员；跨 owner 仍需 8220 重测 |
| 2026-07-19 历史入口/流传输 | Failed（旧环境） | Android 只能通过测试启动入口恢复 conversation 64；SSE 曾被分类为 interruption | `testing/.artifacts/2026-07-19-agent-llm-live-recheck/30-turn/android/`、`02-stream-raw.sse`、`03-audit.json` | 设备和 API 旧，问题类型仍应纳入 C 的历史/流恢复验收 |
| 2026-07-19 image provider | Blocked（旧环境） | provider URL/key/model 未配置，HTTP 422 | `testing/.artifacts/2026-07-19-deployment-config-audit/image-precheck-body.json` | 仍是独立 Deferred/Blocked lane |
| 2026-07-31 predeploy stream | Failed（旧环境） | 客户端 SSE 在 tool_completed 后缺少 answer_delta、answer_completed、run_completed，服务端 audit 后来显示 completed | `testing/.artifacts/2026-07-31-server-agent-predeploy/predeploy-20260731T151657Z-server-config/03-raw.sse`、`03-audit.json` | 是“服务端审计完成但客户端终态不完整”的直接重测输入 |
| 2026-08-01 最新源码部署门 | Blocked/Failed（旧环境） | 154 运行镜像和本地源码迁移版本不一致；隔离 V28 启动曾因 `payload TEXT` 与 `@Lob` 映射失败 | `testing/.artifacts/2026-08-01-server-agent-eval/` | 只作部署 provenance 参考，不在 C 的当前 Agent 结果中计数 |
| 2026-07-29 Web/Android source-only 记录 | Expired | Web build/source-only 记录和 Android 无 ADB 记录都没有当前运行时证明 | `testing/Agent/功能测试/live_execution_ledger.csv` 对应旧行及其 artifacts | Web 首阶段已有独立报告；历史 Agent 审查不将它们当作当前 Agent runtime 结果 |

详细状态索引和 C 登记见：

- `testing/.artifacts/2026-08-22-agent-history-review-B/01-status-inventory.tsv`
- `testing/.artifacts/2026-08-22-agent-history-review-B/02-c-retest-register.tsv`

## 8220 十个 HTTP 200 失败 case

以下 10 个 case 都必须在 C 的当前 8220 账号上重跑。每项都要保存请求、响应、工具轨迹、正式回答、audit、业务表前后计数、draft 前后计数和清理结果。HTTP 200 只算传输成功，不能直接算 Agent 成功。

| case | 现象 | C 的输入 | 通过条件 |
|---|---|---|---|
| `AG-FT-BE-ALL-009` `customer_receivable_lookup` | 目标工具 completed，但正式回答为空 | `哪些客户还欠我钱？按优先收款帮我排一下。` | 目标工具选中且完成；回答非空并引用应收证据；无业务/草稿变化；audit 有完整终态 |
| `AG-FT-BE-ALL-012` `generate_poster_prompt` | 只选了 `product_catalog_lookup`，未选目标工具 | 先保留历史原句 `帮我用商品“美尚热水器”写一段海报提示词，先不要生成图片。`；再用当前目录中真实存在的商品名做控制样本 | 目标工具被选中且完成；只返回提示词，不调用生图；正式回答、audit 和清理完整。若历史商品不存在，必须记录数据前置差异，不得假定通过 |
| `AG-FT-BE-ALL-016` `inventory_low_stock_lookup` | `smart_restock_lookup` completed，但正式回答为空 | `哪些商品快没货了？顺便看看该补多少。` | 允许的低库存/补货工具组合与契约匹配；回答非空、数值有证据；无业务/草稿变化；audit 完整 |
| `AG-FT-BE-ALL-041` `supplier_payable_lookup` | 目标工具 completed，但额外调用 `purchase_order_lookup`，导致工具集合不匹配 | `我还欠哪些供应商钱？金额和采购情况一起看看。` | 明确记录契约允许的工具集合；目标工具完成；若允许采购补充查询则登记为批准序列，否则额外工具仍 Failed；回答、audit、清理完整 |
| `AG-FT-BE-ALL-048` `create_inventory_count_draft` | 只查商品，未生成草稿 | 保留历史原句；先从当前 owner 目录取得真实商品 ID/名称，必要时使用获批最小 fixture：当前库存 7 件，要求先生成盘点草稿 | 目标创建工具完成；`draft_delta=1` 且字段有效；业务表不变；正式回答说明待确认；audit 与删除草稿成功 |
| `AG-FT-BE-ALL-049` `create_pay_order` | 只查供应商，未生成付款草稿 | 使用当前 owner 的真实供应商 ID/名称，金额 `1.23`，备注 `全量工具测试`，明确“先做草稿，不直接付款” | 目标创建工具完成；只新增草稿，不新增 payment；draft 内容含 supplier/amount/note；audit、删除草稿和会话清理成功 |
| `AG-FT-BE-ALL-051` `create_purchase_order` | 只查供应商和商品，未生成采购草稿 | 使用当前 owner 的有效 supplier/product，数量 `1`、单价 `1.23`，要求先做采购草稿 | 目标创建工具完成；草稿含有效 supplier、product line、quantity、price；purchase_orders 不变；正式回答和 audit 完整；清理成功 |
| `AG-FT-BE-ALL-052` `create_purchase_receipt` | 只查采购单，未生成入库草稿 | 使用当前 owner 的有效采购单和明细，入库 `1` 件，明确“不直接记账” | 目标创建工具完成；draft 有采购单/明细/数量；库存、入库和采购业务表不变；正式回答、audit、草稿删除完整 |
| `AG-FT-BE-ALL-053` `create_purchase_return` | 只查采购单，未生成退货草稿 | 使用当前 owner 的有效采购单/商品，退 `1` 件，原因 `全量工具测试`，先生成草稿 | 目标创建工具完成；草稿字段有效；库存/采购退货业务表不变；audit 和删除成功 |
| `AG-FT-BE-ALL-054` `create_sale_order` | 只查客户和商品，未生成销售草稿，且历史名称在当前目录未命中 | 使用当前 owner 的真实 customer/product，数量 `1`、单价 `1.23`，明确“先生成销售草稿” | 目标创建工具完成；草稿有正 customer_id 和有效商品行；sale_orders 不变；正式回答、audit、清理完整 |

原始 case JSON 都位于：
`testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/full-tools-evidence/cases/`，对应文件名为 `009-customer_receivable_lookup.json`、`012-generate_poster_prompt.json`、`016-inventory_low_stock_lookup.json`、`041-supplier_payable_lookup.json`、`048-create_inventory_count_draft.json`、`049-create_pay_order.json`、`051-create_purchase_order.json`、`052-create_purchase_receipt.json`、`053-create_purchase_return.json`、`054-create_sale_order.json`。

## C 必须重新执行的专项 case

### 1. 历史恢复

输入和顺序：

1. 使用当前 owner 2 创建一个带唯一标题的临时 conversation。
2. 发送一个只读商品查询，保存 `conversation_id`、`run_id`、回答和 audit。
3. 分别读取 `/v2/agent/conversations`、workbench、该会话 messages、run-traces；再用显式 `conversation_id` 继续发送第二条消息。
4. 页面或客户端恢复时，检查消息顺序、assistant 正式回答、工具事件和 run-trace 是否一致。
5. 删除临时会话，确认会话和消息计数回到测试前状态。

验收：列表必须能找到会话；messages 不能返回空数据；显式会话 chat 不能返回 400；恢复后的消息、audit 和 run-trace 使用同一会话/owner；删除后无临时残留。

原始证据：

- `testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/case-product-catalog.txt`
- `testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/case-product-catalog-retry.txt`
- `testing/.artifacts/2026-08-20-8220-agent-retest/auth-history/wave1-readonly-api.txt`

### 2. 跨 owner

当前 8220 只有 owner 2/store 1/一个 membership，历史 owner 7、8 的 154 结果不能替代本项。C 需先取得第二个有效 owner、其 store、membership、session 和授权；没有这些前置时结果只能登记 Blocked。

输入和顺序：


1. owner A 和 owner B 分别登录，记录各自 owner/store，不输出 token。
2. 在 owner A 范围准备一个可识别的最小商品或选取已有唯一商品；若创建 fixture，记录 before/after 和删除结果。
3. 两个 owner 分别执行 `我这家店现在有几个商品？` 和同一只读工具查询。
4. owner B 尝试读取 owner A 的商品、conversation、draft、audit ID；owner A 读取自己的对象作对照。
5. 删除 fixture 和临时会话，确认两边计数和列表恢复。

验收：B 的 Agent 工具只能返回 B 的 owner scope；跨 owner 资源返回契约规定的 403/422；A 的列表和 audit 不泄露给 B；无跨 owner 业务/草稿变化。

原始证据：

- 当前 Blocked：`testing/.artifacts/2026-08-20-8220-agent-retest/auth-history/wave1-readonly-api.txt`
- 旧 154 对照：`testing/.artifacts/2026-08-03-server-sync-scope/remote-sync-scope.jsonl`、`remote-sync-scope.raw.tsv`

### 3. SSE、cancel 和 audit

完成流输入：当前 owner 2 发起只读库存/商品查询，使用 `/v2/agent/chat/stream`，保存原始 SSE、HTTP 状态、run_id 和 conversation_id。验收 `run_started -> tool_started/tool_completed -> answer_delta -> answer_completed -> run_completed` 顺序合理，HTTP 200，回答非空，audit 终态与事件一致，`audit_lossy=false`，临时会话可删除。

取消流输入：沿用当前 8220 取消样本的长问题 `请把所有商品的库存、近期销量和补货建议都列出来，并详细解释每一项。`，收到 `run_started` 后立即调用 cancel。验收收到 `run_cancelled`，cancel HTTP 200，不能伪造正式回答，通常不应再出现 `run_completed`；GET audit 必须为 cancelled，事件数与 emitted_event_count 一致，删除 HTTP 200。

失败回答审计输入：从上面 10 个失败 case 中至少选 009、016、041 和一个草稿失败 case，使用返回的 run_id GET audit。验收失败原因、工具 completed/未选中状态、正式回答缺失、draft_id 和终态都可追溯；不能只凭 chat envelope 判断审计完整。

原始证据：

- 当前取消：`testing/.artifacts/2026-08-20-8220-agent-retest/stream-cancel/mg-8220-sse-retest-20260820/`
- 当前完成并发样本：`testing/.artifacts/2026-08-20-8220-agent-retest/performance/mg-8220-sse-concurrency-20260820/`
- 旧客户端终态缺失对照：`testing/.artifacts/2026-07-31-server-agent-predeploy/predeploy-20260731T151657Z-server-config/03-raw.sse`、`03-audit.json`

### 4. 并发

SSE 输入：10 个独立临时 conversation，使用同一只读问题或等价短问题，保留每路 raw SSE 和每路 audit。验收每路都收到 run_started、run_completed、非空回答和删除 200；run_id、conversation_id、owner 不串线；汇总 P50/P95/max，并注明没有 SLA。

非流式输入：30 轮 sequential `/v2/agent/chat`，每轮新临时会话或按 runner 既有隔离方式执行，保留 HTTP、answer_present、audit 和 delete 结果。验收 30/30 的完整性逐轮成立，任何 timeout、空回答、清理失败都单独列出，不能以平均值掩盖。

原始证据：

- `testing/.artifacts/2026-08-20-8220-agent-retest/performance/mg-8220-sse-concurrency-20260820/results.tsv`
- `testing/.artifacts/2026-08-20-8220-agent-retest/performance/mg-8220-sse-concurrency-20260820/summary.txt`
- `testing/.artifacts/2026-08-20-8220-agent-retest/performance/nonstream-30-summary.txt`
- 旧超时对照：`testing/.artifacts/2026-08-03-production-agent-recheck-154-deployed/soak-20260803-approved-final-v2/`

### 5. 清理

会话/草稿输入：使用一个成功创建草稿的 case 和一个失败工具选择 case，分别记录 `agent_conversations`、messages、drafts、业务表和 audit before/after；对成功草稿执行取消、确认（如适用）、删除，对失败 case 直接删除临时会话。验收业务表无意外写入，草稿计数恢复，临时会话和媒体绑定没有残留，audit 保留所需终态。

媒体输入：使用最小无敏感临时文件执行 upload -> metadata -> binding（如需要）-> delete，再检查 API、数据库记录、binding 和精确文件路径。验收删除响应、数据库和文件系统同时归零；禁止用手工删除后的最终计数替代自动删除验证。

原始证据：

- `testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/full-tools-evidence/cases/057-media_upload_tool.json`
- `testing/.artifacts/2026-08-20-8220-agent-retest/database/media-api-case.txt`
- `testing/.artifacts/2026-08-20-8220-agent-retest/database/final-state.txt`
- 全量清理计数：`testing/.artifacts/2026-08-20-8220-agent-retest/readonly-agent/full-tools-evidence/summary.json`

## 旧环境和过期记录处理

- 154 服务器和其 provider/runtime 证据统一标为旧环境；其 Passed 结果保留作输入模板，不能写入当前 8220 通过率。
- `https://sxyq27.online/zhj-api/`、2026-07-19 Android device `d715a3a4` 和 2026-07-31 predeploy image 都没有当前 8220 provenance，必须重新获取当前 host/container/source/provider 证据。
- 2026-08-01 的最新源码部署门、隔离迁移失败、远程 SSH 阻塞仍是部署前置风险，C 不得把本地测试或旧镜像结果当作当前 Agent runtime。
- 多模态和 image provider 记录保持 Deferred/Blocked，等 provider 和测试资料具备后单独建 case。

## `*-e` 临时副本

本轮检查了：

- `testing/web/功能测试/web_action_audit_20260822_B.csv-e`
- `testing/web/功能测试/阶段报告-20260822-Web按钮Agent-B.md-e`
- `testing/.artifacts/2026-08-22-web-buttons-B/04-case-register.jsonl-e`

三个文件都与对应的已提交文件拥有相同记录内容，差异仅是文件末尾多一个空行，因此 SHA-256 不同但没有新的 case 或结论。它们属于首阶段临时副本，本次历史提交明确排除；没有删除任何文件。

## 交接文件

- 本文：`testing/web/历史Agent交接-20260822-B.md`
- 状态索引：`testing/.artifacts/2026-08-22-agent-history-review-B/01-status-inventory.tsv`
- C 重测登记：`testing/.artifacts/2026-08-22-agent-history-review-B/02-c-retest-register.tsv`
- 证据映射：`testing/.artifacts/2026-08-22-agent-history-review-B/03-evidence-index.md`
- 临时副本核对：`testing/.artifacts/2026-08-22-agent-history-review-B/04-e-copy-review.md`

C 执行时必须逐条登记 `test_id、env、owner/store、pre_state、input、expected、actual、artifacts、cleanup、result`，结果只使用 `Passed`、`Failed`、`Blocked` 或 `Deferred`。
