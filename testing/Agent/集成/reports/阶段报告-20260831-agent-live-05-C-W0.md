# Stage Report: 20260831-agent-live-05-C-W0

## Scope

- This report covers read-only remote Revive and Android emulator preparation only.
- Deployment, build, database cleanup, login, Agent prompts, Provider calls, REST Agent calls, SSE, and performance focused flows were not executed.
- The required deployment source is the later A/B/D merged source. D is still pending.

## Results

| test_id | area | result | evidence |
| --- | --- | --- | --- |
| AG-I-012 | 8220 containers, compose, image, PostgreSQL, migration and safe key-presence checks | Blocked | `testing/Agent/集成/artifacts/20260831-agent-live-05-C-AG-I-012/` |
| AG-CLI-AND-001 | AVD boot, package/activity resolution and empty login screen | Blocked | `testing/Agent/客户端/artifacts/20260831-agent-live-05-C-AG-CLI-AND-001/` |

Wave totals: total=2, Passed=0, Failed=0, Blocked=2, Deferred=0.

## Remote observations

- SSH to 8220 succeeded with the existing local key as `root`; private key material was not printed.
- `sxyq27-zhj-api`, PostgreSQL, Redis, Docker and Nginx were active. The current API container image was `sxyq27-zhj-api:20260830T183500-admin-usage-f8361754`.
- `/opt/sxyq27/master-goods` contained `compose.yml` and release material but no `.git` metadata. No remote source commit was attributable to A/B/D.
- PostgreSQL `zhj` was reachable read-only; Flyway latest marker was `41` and selected Agent/media table counts were all `0`.
- Protected configuration keys were detected by name only. `AGENT_MODEL` and `AGENT_CONTEXT_MAXIMUM_WINDOW` were absent from the container environment. No secret value was captured.
- Public GET probes returned `401` for `/` and `/actuator/health`; response bodies were not saved.

## Android observations

- `Zhihuiji_API34` booted on `emulator-5554` with `sys.boot_completed=1`, Android 14/API 34, 720x1280.
- `com.zhihuiji.app` version `1.0.0` and `.MainActivity` resolved successfully.
- The initial screen had empty phone/password fields. No click or text input occurred.
- Static release source points to `https://zhj-api.sxyq27.online/` and enforces HTTPS. Runtime URL routing is not confirmed because no app request was sent.
- No safe login state or user-provided development password was available. Login-dependent tests are blocked/deferred.

## Next gate

Wait for D. Verify A/B/D ancestry and caller regression tests, then build and deploy the exact merge source. Keep current image evidence separate; do not treat it as the A/B/D deployment.
