# App observation

- ADB final state: `List of devices attached` with no device row.
- The AVD connected transiently during launch but exited before `sys.boot_completed=1`; the retry had the same result.
- No stable app window, UI tree, screenshot, login form submission, or Agent screen was available.
- This text is an observation of absence, not a fabricated UI artifact.
