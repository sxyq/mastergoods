# 2026-09-06 Wave 46 Android 真机登录与 AG-CLI-AND-003

## 结论

线上账号恢复后，物理设备 `d715a3a4` 通过 Android UI 登录成功。随后真实进入 AI 对话、输入并发送非零年度销售与付款分析请求；App 收到 HTTP `200`、`text/event-stream`，但最终展示“暂时无法完成这次请求，请稍后重试”，没有图表。`AG-CLI-AND-003` 的非零图表分支本轮 `Failed`，其他未收敛用例未在本轮启动。

## 环境与登录

- 设备：物理 Android `d715a3a4`，型号 `25010PN30C`；未启动模拟器。
- App：`com.zhihuiji.app` 1.0.0，入口 `com.zhihuiji.app/.MainActivity`。
- 运行配置：`https://oneapi.sxyq27.online/v1`、`deepseek-v4-flash-0731`、`chat_completions`。
- 真实登录按钮 bounds `[116,1429][964,1555]`，点击中心 `(540,1492)`；登录 HTTP 200，随后首页接口 HTTP 200。
- API Key、密码、Authorization、Cookie 和完整认证载荷未写入证据。

## AG-CLI-AND-003 真实 UI 结果

1. 通过底部“助手”真实点击进入 AI 对话，再通过“新建对话”真实点击创建独立会话。
2. 依据提交前 UI tree 的输入框和发送控件 bounds 输入 ASCII 测试语句 `Analyze sales trends payments show chart last year`，并真实点击发送中心 `(966,1425)`。
3. 2 秒、10 秒、25 秒分别采集 UI tree 与截图。App 日志记录 `POST /v2/agent/chat/stream` HTTP 200、`Content-Type: text/event-stream`；最终 UI 没有 `result_visualization`，显示可重试错误。

| 子项 | 状态 |
|---|---|
| 登录与首页恢复 | `Passed` |
| 真实 Agent UI 进入、输入、发送 | `Passed` |
| SSE HTTP 入口 | `Passed`，HTTP 200 / `text/event-stream` |
| 非零数据图表与 facts 一致 | `Failed`；最终错误，没有图表或完整工具事实可验 |
| AG-CLI-AND-003 综合 | `Failed` |

## 清理与证据

- 通过 App 的“清空对话”、会话列表中的“删除会话”完成本轮会话清理；随后 `pm clear com.zhihuiji.app` 返回 `Success`，重新启动后回到登录页。
- 服务端清理后：`agent_conversations=17`、`agent_messages=47`、`agent_drafts=0`、`agent_run_audits=93`、`agent_run_audit_events=1477`、`agent_context_checkpoints=0`；`products=693`、`customers=84`、`finance_records=2661`。
- crash buffer 0 行。
- 证据目录：`testing/Agent/客户端/artifacts/20260906-agent-phase2-wave46-physical-login-and-003-034-035-036-037-038-039/`。
- 关键证据：登录 UI/截图、`06-login-logcat-safe.txt`、`08-server-before-ag003.txt`、`13`–`18` 输入 UI/截图、`19`–`26` 发送 UI/截图与 crash、`27-ag003-latest-audit-safe.txt`、`30`–`35` 会话清理 UI/截图、`36-pm-clear.txt`、`37`–`40` 最终 UI/截图/服务端计数。

本轮未启动模拟器，未重置账号密码，未修改 API Key、源码或线上配置。`AG-CLI-AND-004/006/007/008/010` 本轮未执行，保持既有状态。
