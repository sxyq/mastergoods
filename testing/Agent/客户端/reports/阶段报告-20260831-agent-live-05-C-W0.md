# Client Preparation Report: 20260831-agent-live-05-C-W0

## Result

Android preparation completed at the environment level, with the login flow blocked. No production client source was changed and no APK was built or installed.

## Verified

- AVD `Zhihuiji_API34` is running on `emulator-5554`.
- `sys.boot_completed=1`, Android 14/API 34, display 720x1280.
- Package `com.zhihuiji.app` is installed at version `1.0.0`.
- `com.zhihuiji.app/.MainActivity` resolves as the launcher activity.
- The initial UI is an empty login screen. UI XML, summary, screenshot, and redacted logcat are saved in the artifact folder.
- Static release configuration names `https://zhj-api.sxyq27.online/` as the default endpoint and sets cleartext release access to false.

## Not executed

- No click, swipe, text input, login, token use, Agent request, SSE, tool call, audit lookup, database operation, or local `18080` access.
- Runtime endpoint confirmation is still pending an allowed login and a deployed A/B/D build.
- Android performance trace collection is not claimed; no focused flow was available.

Result for `AG-CLI-AND-001`: Blocked at the time of this preparation capture. A later A/B fallback deployment authorization is recorded separately; this client artifact remains pre-login and has no runtime request evidence.
