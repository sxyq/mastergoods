# Agent 性能测试规划（性能）

更新日期：2026-08-28。性能测试先采集真实样本，再与产品 SLA 或同版本基线比较；无正式 SLA 时只形成可复现基线并记 `Deferred`，不能自行把基线当成通过标准。轮次与工具预算事实见 [../代码事实基线.md](../代码事实基线.md)（轮次硬上限 6、单 run 工具调用预算 12、压缩 20s/摘要上限等）。

## 一、判定通则

- 只使用有效请求作为样本；认证失败、Schema 拒绝、无权限、Provider 不可用、设备缺失单独统计，不混入成功百分位。
- 每个样本记录：样本编号、run_id、owner/store 脱敏标签、工具数、循环轮数、上下文预算字段、工具调用数、SSE 事件数、数据库查询计数、资源采样时间。
- 每个场景至少保留预热样本、正式样本和异常样本；正式百分位不包含认证失败、参数拒绝、权限拒绝、Provider 不可用或设备缺失。
- 并发测试同时记录并发级别、成功/失败/阻塞数量、5xx 数、连接数、线程数、数据库连接池和 JVM 内存；只记录聚合值，不保存凭据或完整业务载荷。
- 初始门槛：有效请求 5xx=0；SSE 合法终态唯一；事件丢失/重复按场景规则为 0；创建与确认无重复业务记录；跨 owner/store 串线=0。门槛是起点，不是通过条件。
- 生产 PostgreSQL 不可用时，分页性能只能准备 SQL/脚本，真实 `EXPLAIN` 记 `Blocked`/`Deferred`。

## 二、专项用例（AG-P-001~027，初始 `Deferred`）

| 编号 | 场景与样本 | 执行方法 | 关键指标 | 初始验收 |
|---|---|---|---|---|
| AG-P-001 | 无工具基线 | 30 个有效请求，并发 1；短问题分别 REST/stream | REST P50/P95/P99；TTFB、首事件、完成时间；CPU/内存 | 无 5xx；流式有合法终态；形成当前部署基线 |
| AG-P-002 | 单只读工具 | 46 工具各 10 次非流式 + 10 次流式 | 每工具 P50/P95/P99、工具耗时、Repository 查询数、返回字节数 | 每个工具独立分布；功能正确样本才进统计 |
| AG-P-003 | 参数复杂度 | 简单/多筛选/大结果各 20 次 | 参数解析、查询、序列化耗时与结果大小 | 过滤增加不引起异常增长；超限预拒绝 |
| AG-P-004 | 创建草稿 | 14 创建工具各 20 次（仅草稿） | 首草稿事件、草稿完成、总时延、草稿表写入数 | 每次最多一个草稿；无正式表写入 |
| AG-P-005 | 确认写入 | 每类创建动作 20 次成功确认 | 确认 P50/P95/P99、事务耗时、锁等待、提交/回滚 | 成功确认只产生预期记录；失败无半写入；无 500 |
| AG-P-006 | SSE 首事件 | 短/单工具/多工具/创建各 30 次 | TTFB、first_event、first_tool、first_answer | 首事件可见；P95 与基线比较；无空流成功 |
| AG-P-007 | SSE 完整流 | 每输入 30 次 | 完成时延、事件丢失率、重复事件率、字节数 | 丢失率=0、重复率=0（重连按去重规则）；终态唯一 |
| AG-P-008 | 多工具与 Loop | 2/3/4/6 工具计划各 20 次 | 每轮耗时、工具总耗时、模型耗时、循环轮数、调用数 | 轮次不超 AgentIterationPolicy 预算（≤6）；单 run 工具调用不超 12；无无界循环 |
| AG-P-009 | 上下文不压缩 | 20 个短会话 × 10 轮 | 上下文构建耗时、输入估算、Provider 时延 | 不触发压缩时不调用压缩 Provider |
| AG-P-010 | 上下文压缩 | 10 会话 × ≥30 轮，≥3 完整轮 | 触发率、压缩耗时、确定性/语义比例、checkpoint 保存/复用 | 触发边界稳定；当前问题/权限/工具状态保留；失败可降级 |
| AG-P-011 | 超长问题 | 10 个超预算 + 10 个接近预算 | 输入 token、拒绝时延、内存峰值、错误终态 | 超预算不静默截断；稳定 4xx/`EXHAUSTED`；无业务写入 |
| AG-P-012 | 多用户流并发 | 并发 1/5/10/20 × 30 有效请求 | 吞吐、P50/P95/P99、5xx、连接、线程、DB pool、CPU/内存 | 无串租户/串会话；资源与 P95 与 SLA/基线比较 |
| AG-P-013 | 同一会话并发 | 并发 2/5/10 × 20 组 | 排队时延、乱序数、重复运行数、锁等待 | 消息/工具/回答不交叉；run 绑定正确 |
| AG-P-014 | 草稿确认竞争 | 2/5/10 并发确认 × 20 草稿 | 2xx/409/业务失败比例、500、正式记录数、锁等待 | 500=0；同草稿正式记录≤1；终态合法 |
| AG-P-015 | 重复请求 | 顺序重复/网络重试/重复点击各 20 组 | 去重命中、重复写入、冲突码、响应一致性 | 同一幂等语义一个结果；payload 冲突明确 |
| AG-P-016 | 取消时延 | 首事件前/工具中/回答中/完成后各 20 次 | cancel RTT、残余事件、Provider 中止、正式写入 | 取消后无新回答增量；未确认创建不写正式表 |
| AG-P-017 | 断线重连 | 每断点 20 次 | 恢复成功率、重复/丢失事件率、完成时间 | 可恢复只补缺失；不可恢复明确终态；不重复写入 |
| AG-P-018 | 结果块规模 | 小/中/大各 30 次（KPI/表格/趋势/搜索/组合） | payload 字节、序列化、传输、客户端解析、首屏 | 无截断、OOM、崩溃；图表与 facts 一致 |
| AG-P-019 | Provider 慢响应 | 正常/延迟/超时/429 各 20 次 | Provider 时延、重试次数、总时延、错误终态、资源释放 | 失败不伪装成功；重试有上限；不重复工具与写入 |
| AG-P-020 | 搜索性能 | `result_limit` 取 0/1/5/10/11 各 20 次；Provider 额外返回 50/100 条作为截断样本 | Provider 时延、解析时延、payload、来源数、截断数 | 请求上限 10；11 和非法值在工具执行前拒绝或按现行 4xx；Provider 超量结果不得突破上限 |
| AG-P-021 | 数据库分页 | 小/中/大数据量各 20 次；无 PG 只准备 | SQL 数、返回行数、DB 时延、内存峰值、查询计划 | 禁止全量查询后内存分页；真实 EXPLAIN 缺条件记 Blocked/Deferred |
| AG-P-022 | 长会话 Soak | ≥30 分钟或每并发级累计 500 有效请求 | P95 漂移、吞吐、5xx、SSE 丢失/重复、JVM 堆、线程、DB pool | 无持续内存增长/连接泄漏/串会话/时延失控 |
| AG-P-023 | Android Agent 展示 | 设备具备时每核心流程 10 次 | 首屏、首工具可见、帧率/掉帧、APP 内存 | 展示与服务端一致；无设备记 Blocked |
| AG-P-024 | iOS Agent 展示 | 设备具备时每核心流程 10 次 | 首屏、首事件、渲染、内存、断线恢复、重复事件 | 同上；签名/Xcode 缺失记 Blocked |
| AG-P-025 | 记忆召回/提取 | 记忆存在时 20 次 | 召回时延、异步提取耗时、DB 写入 | 不阻塞主回答；召回失败只丢弃记忆块 |
| AG-P-026 | 多模态图片性能 | 1/9 张附件各 10 次（Provider 可用时） | 图片读取/编码、Provider 时延、首事件 | 附件数量边界内；失败不产生半写入 |
| AG-P-027 | Agent 生图资源与并发 | `image_generate` 草稿/拒绝/确认；并发 1/5/10；Provider Mock 正常/慢响应/超时/取消；参考图 0/1 张 | 草稿创建时延、确认总时延、Provider 调用数、连接/线程/内存、超时率、取消 RTT、临时资源和重复结果数 | 工具阶段 Provider 调用 0；确认最多一次调用；超时/取消资源释放；无重复结果；真实 Provider 费用/配额、客户端 UI 和生产 PostgreSQL 查询计划记 `Blocked`/`Deferred`；初始 `Deferred` |

## 三、指标定义

| 指标 | 起止点 |
|---|---|
| `request_latency_ms` | 发送 → REST 响应结束 |
| `ttfb_ms` | 发送 → 收到响应头/首个 SSE 字节 |
| `first_event_ms` / `first_tool_ms` / `first_answer_ms` | 发送 → 首个合法事件 / `tool_started` / `answer_delta` |
| `completion_ms` | 发送 → 合法终态 |
| `tool_duration_ms` | `tool_started` → `tool_completed/tool_failed`（call_id 配对） |
| `compaction_ms` | 压缩开始 → 检查点可用 |
| `error_rate` / `event_loss_rate` / `duplicate_event_rate` | 按场景、并发级别统计 |
| `memory_delta` | 场景前后 JVM/APP 内存差值 |

## 四、统计与判定

| 项目 | 规则 |
|---|---|
| 百分位 | 同一场景、同一并发级别、同一版本分别计算 P50/P95/P99；样本不足时只报告样本量，不给百分位结论 |
| 稳定性 | 报告 `5xx/request`、超时率、合法终态率、事件丢失率、重复率、资源未释放数和重复业务记录数 |
| 资源 | 记录 JVM 堆、线程、连接池、数据库查询数；客户端性能仅在对应设备条件具备时记录 |
| 对比 | 优先与产品 SLA 或上一部署版本比较；无基线时输出测量结果并标 `Deferred`，不写“达标” |
| 生产计划 | 无 PostgreSQL 时只保留 SQL 和执行步骤；`EXPLAIN/EXPLAIN ANALYZE` 标 `Blocked` 或 `Deferred` |

## 五、已知基线关注点（来自现行历史问题，涉及性能）

- 32 消息长历史滚动卡顿与内存增长（jank 16.76%、PSS +32MB）此前失败；Soak/长会话用例必须复测并记录。
- 30 轮只读长会话此前在第 16 轮被写入频率限制误拦截；只读长会话必须带否定语义回归（见 安全/TEST_PLAN.md AG-S-027）。
- SSE 取消时序此前出现“取消后 audit 落 failed/STREAM_ERROR”；取消时延用例必须核对 audit 终态为 cancelled。

## 六、证据存放

- `性能/artifacts/<日期>-<波次>-<用例>/`：样本明细、百分位、资源与错误统计（README 第六节文件序列）。
- 脚本：`../脚本/性能/`；并发工具与探针不提交凭据。

`AG-P-027` 每条记录必须包含输入、并发级别、Provider Mock 响应类型、P50/P95/P99（样本足够时）、资源聚合值、草稿/正式表 before-after、audit、清理证据和 `Passed/Failed/Blocked/Deferred` 状态。真实 Provider 仅在获批的隔离环境执行。
