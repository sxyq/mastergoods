# App observation

- `test_id`: `AG-F-LIVE-LAZY-ROUTE-001`
- `category_id`: `AG-F-ENV-LAZY`
- `wave_id`: `Wave 0`
- `result`: `Blocked`
- `source`: independent local HTTP client; this batch is not Android UI evidence.
- Android session: not used; the emulator remained on the login screen and supplied no `run_id`.
- The service responded at `18080`, but every protected probe was rejected before Agent execution.
