Purpose: unauthenticated, read-only Android API smoke probes against the current configured HTTPS base URL.

Target source:
- Code/frontend/android/core/datastore/src/main/java/com/zhihuiji/core/datastore/SettingsStore.kt: DEFAULT_BASE_URL
- https://zhj-api.sxyq27.online/

Request policy:
- No Authorization, Cookie, refresh token, request body, multipart part, or write method is sent.
- GET route placeholders are replaced with 0 only for route-shape probing.
- Query parameters are omitted for the generated route sweep; representative query-shape probes are separate.
- TLS certificate verification is bypassed only for this diagnostic curl probe; no application configuration is changed.
- Response headers and bodies are stored in the artifact directory. Any credentials found in output must be redacted before reporting.

Generated route sweep command:

    base='https://zhj-api.sxyq27.online'
    perl -ne 'while (/\@GET\((?:value\s*=\s*)?"([^"]+)"/g) { print "$1\n" }' Code/frontend/android/core/network/src/main/java/com/zhihuiji/core/network/ZhihuijiV2Api.kt | sed -E 's/\{[^}]+\}/0/g' | sort -u
    curl --silent --show-error --insecure --connect-timeout 10 --max-time 25 -H 'Accept: application/json' "$base/<route>"

Expected interpretation:
- 2xx with no authentication would be unexpected and requires response-contract review.
- 401/403 confirms the route is reachable but does not validate an authenticated success contract.
- 404/405/422/5xx is retained as live evidence and compared with source expectations.
- Network/TLS/DNS failure is Blocked for HTTP execution.
