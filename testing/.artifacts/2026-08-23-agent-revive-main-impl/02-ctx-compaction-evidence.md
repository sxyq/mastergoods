# P0 上下文压缩与检查点证据 — 2026-08-23

- 代理 ID：main-impl
- 目录：testing/.artifacts/2026-08-23-agent-revive-main-impl/
- 分支：codex/publish-local-updates

## 修复内容

1. **压缩后当前请求仍使用旧消息列表**（V2AgentAiService）：非流式与流式路径统一改为
   `afterBoundary(messagesAfterBoundary, compaction.boundaryMessageId())`，压缩发生后
   当前请求只使用新检查点边界之后的原始消息 + 检查点摘要 + 当前问题。
2. **测试构造器**：V2AgentAiServiceTest 增加 ContextBuilder / ContextCompactionService
   真实组件（mock 仓储 + 真实窗口解析/估算），恢复 compileTestJava。
3. **检查点复用语义修正**（ContextCompactionService）：预算超限时不再因
   hasActiveCheckpoint() 直接复用，按计划 6.8 继续压缩边界后历史，生成边界更新的检查点。
4. **失效后同一边界重建**：saveCheckpoint 在唯一约束冲突且无有效版本时提升 revision
   重试（上限 3 次），支持失效后重新生成有效检查点；并发冲突仍回退读取已提交版本，
   不把并发异常返回为未处理 500。
5. **确定性摘要脱敏**：手机号（1[3-9]\d{9}）、API Key / password / secret / token 长凭据
   在写入摘要前脱敏（保留前后缀 + 星号），满足计划 6.7 敏感信息约束。

## 新增测试（全部通过）

| 测试类 | 覆盖 |
|---|---|
| ContextWindowResolverTest | 配置按模型覆盖、unknown model 保守窗口、configuredMaximum 截断、降级判定 |
| TokenEstimatorTest | 空文本、字符比例、JSON/消息列表/历史文本开销、超长截断 |
| ContextBuilderTest | 预算比例、超长问题触发压缩、检查点边界加载、无检查点最近消息正序、降级安全余量、owner/conversation 隔离 |
| ContextCompactionServiceTest | 预算充足不压缩、确定性降级摘要、有效语义摘要、无效语义摘要回退、Provider 异常回退、并发冲突读已提交、失效后 revision 重建、轮次不足不压缩、当前轮保留、敏感信息脱敏 |
| V32AgentContextCheckpointsSqlTest | H2 PostgreSQL 模式 DDL、唯一约束、失效后重建、owner 隔离、级联删除；SQLite 无法解析 IDENTITY 语法（Blocked 记录） |
| AgentContextCheckpointRepositoryTest | @DataJpaTest：owner 隔离、唯一约束冲突、失效后 revision 重建、invalidateAfterBoundary 边界语义、清理 |
| V2AgentAiServiceTest 新增 | chatAfterCompactionUsesOnlyBoundaryAfterMessagesForCurrentRequest（压缩后历史只保留边界之后消息 id=7）、streamCompactionEmitsContextCompactedAndKeepsBoundaryAfterMessages（context_compacted 事件 + 确定性摘要无凭据） |

## 验证命令与结果

- `./Code/backend/gradlew -p Code/backend test --offline --tests "...agent.context.*" --tests "...V32AgentContextCheckpointsSqlTest" --tests "...AgentContextCheckpointRepositoryTest"` → BUILD SUCCESSFUL（50 tests）
- `./Code/backend/gradlew -p Code/backend test --offline --tests "...V2AgentAiServiceTest"` → BUILD SUCCESSFUL（75 tests，含新增 2 个）
- 完整套件：见 `03-backend-full-test.md`（单独记录）

## Blocked / Deferred 记录

| 项目 | 状态 | 依据 |
|---|---|---|
| SQLite 执行 V32 迁移 | Blocked | sqlite-jdbc 内置 SQLite 拒绝 `GENERATED BY DEFAULT AS IDENTITY` 语法（near "AS": syntax error）；Agent 表运行时数据库为 H2(local)/PostgreSQL(prod)，SQLite 仅遗留导入来源 |
| PostgreSQL 生产查询计划 / EXPLAIN | Deferred | 无 PostgreSQL 环境；H2 仅验证查询语义 |
| 真实 Provider 语义压缩 | Deferred | 无 Provider 凭证，确定性降级路径已单测覆盖 |

## 证据文件

- git-working-tree-inventory.md（第一步）
- 02-ctx-compaction-evidence.md（本文件）
