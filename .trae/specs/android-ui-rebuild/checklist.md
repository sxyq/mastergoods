* [ ] 旧 UI 源码已全部删除（feature/*、navigation/*、core/designsystem/\*）

* [ ] 删除后项目骨架能编译（data/core 层无影响）

* [ ] `ZhihuijiColors.kt` 已创建，包含主色 `#1677FF`、成功 `#18B66A`、警告 `#FF9F1A`、危险 `#F04438`

* [ ] `ZhihuijiTypography.kt` 已创建，包含页面大标题 24sp、页面标题 18sp、卡片标题 14sp、正文 13sp、辅助 11sp、金额 22sp

* [ ] `ZhihuijiShapes.kt` 已创建，包含卡片 12-16dp、输入框 10-12dp、按钮 12dp

* [ ] `ZhihuijiTheme.kt` 已创建，作为 Material 3 主题入口

* [ ] `LiquidGlassCard.kt` 已创建，使用 backdrop 库实现 blur + vibrancy + lens + highlight + innerShadow

* [ ] `GlassScaffold.kt` 已创建，包含渐变背景 + 状态栏适配 + 底部导航占位

* [ ] `FloatingLiquidBottomBar.kt` 已创建，五栏底部导航

* [ ] `GlassTopBar.kt` 已创建，支持大标题/居中标题/返回/搜索/筛选/更多

* [ ] `KpiCard.kt` 已创建

* [ ] `StatusPill.kt` 已创建，支持全部状态色

* [ ] `SearchFilterBar.kt` 已创建

* [ ] `SegmentedTabs.kt` 已创建

* [ ] `FilterChipRow.kt` 已创建

* [ ] 按钮体系已创建（PrimaryButton / SecondaryOutlineButton / DangerOutlineButton）

* [ ] `BottomActionBar.kt` 已创建

* [ ] `EmptyState.kt` 已创建

* [ ] `QuantityStepper.kt` 已创建

* [ ] `ChartCard.kt` 已创建

* [ ] `BusinessListItem.kt` 已创建

* [ ] `MainScreen.kt` 已重建，五栏导航正常

* [ ] `MainNavGraph.kt` 已重建

* [ ] `SubNavGraph.kt` 已重建

* [ ] `AppNavGraph.kt` 已更新

* [ ] `LoginScreen.kt` 匹配设计稿 02.png

* [ ] `RegisterScreen.kt` 匹配设计稿 02.png

* [ ] `DashboardScreen.kt` 匹配设计稿 01.png（今日经营 + KPI + 趋势图 + 提醒 + 快捷操作）

* [ ] `SaleOrderListScreen.kt` 匹配设计稿 05.png

* [ ] `SaleOrderDetailScreen.kt` 匹配设计稿 05.png

* [ ] `SaleOrderEditorScreen.kt` 匹配设计稿 01.png/05.png

* [ ] `PurchaseOrderListScreen.kt` 匹配设计稿 06.png

* [ ] `PurchaseOrderDetailScreen.kt` 匹配设计稿 06.png

* [ ] `PurchaseOrderEditorScreen.kt` 匹配设计稿 06.png

* [ ] `ProductListScreen.kt` 匹配设计稿 03.png

* [ ] `ProductDetailScreen.kt` 匹配设计稿 03.png

* [ ] `ProductEditorScreen.kt` 匹配设计稿 03.png

* [ ] `CustomerListScreen.kt` 匹配设计稿 04.png

* [ ] `CustomerDetailScreen.kt` 匹配设计稿 04.png

* [ ] `SupplierListScreen.kt` 匹配设计稿 04.png

* [ ] `SupplierDetailScreen.kt` 匹配设计稿 04.png

* [ ] `ReportScreen.kt` 匹配设计稿 07.png

* [ ] `AgentWorkbenchScreen.kt` 匹配设计稿 08.png

* [ ] `AgentChatScreen.kt` 匹配设计稿 08.png

* [ ] `OperationDraftScreen.kt` 匹配设计稿 08.png

* [ ] `AgentTaskScreen.kt` 匹配设计稿 08.png

* [ ] `SettingsScreen.kt` 匹配设计稿 02.png

* [ ] 项目能编译通过 `./gradlew :app:assembleDebug`

* [ ] Lint 检查无严重错误

