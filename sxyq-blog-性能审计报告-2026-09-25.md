# sxyq-blog 全仓库性能审计报告（第一阶段：只读审计）

- 审计对象：`/Users/sunyiyang/Desktop/Project/sxyq-blog`
- HEAD：`9f28c864`（feat/internal-beyond-frontend-redesign，与 origin/main 同步）
- 审计时间：2026-09-25
- 方法：源码逐文件追踪 + 历史材料（09-02 / 09-13 / 09-23 trellis 任务）重新核实 + 本机合成数据 benchmark（bench-newapi-usage / bench-public-derived / 临时 eventStats 测量）。未改任何产品代码、未部署、未动生产。
- 本机 benchmark 均为 M 系 Mac 数据，日本服务器（1GB 内存）按 2~4× 减速估算，仅用于量级判断。

---

# CURRENT PERFORMANCE MAP

## 链路 1：公共博客（最面向用户的路径）

```
Browser → 124 公网 Nginx(edge cache 60s) → WireGuard(10.77) → 日本 Nginx → Blog API(Express, 容器 127.0.0.1:3000)
                                                            ├─ storage.json(258KB, 进程内缓存, stat 节流 100ms)
                                                            ├─ analytics.sqlite(/home //site 每次未命中请求做全表 COUNT)
                                                            └─ 派生缓存 publicDerived(tagCloud/contentStats)
```
- 主要等待：**跨节点网络路径**（2026-09-25 实测：公网 /home API 1.34–1.57s；日本本机回环 8.9ms；Blog API 本身毫秒级）
- 主要 CPU：eventStats 全表聚合（随事件量线性增长）、公共派生缓存命中后≈0
- 写路径：`POST /track` → analytics.sqlite 追加，**不再写 storage.json**

## 链路 2：Admin（Vue → Blog API → 各远程依赖）

```
Vue Admin → Blog API ─┬─ storage.json（读走进程缓存）
                      ├─ monitoring-bridge(HTTP 10.77.0.1:8788, 缓存10s+single-flight, Beszel SQLite 3次SELECT)
                      ├─ network-performance.sqlite（时间窗 SQL + 20k retention）
                      ├─ NewAPI 渠道代理(HTTP 10.77.0.1:8789, 缓存5s+dedup, 30s超时)
                      ├─ NewAPI 用量（NDJSON/SQLite + usageResultCache 5min TTL，每次采集失效）
                      ├─ VPN Manager(Unix socket → systemctl×2 + ss×2 + trafficStats HTTP×2 + observer SQLite，无缓存)
                      ├─ Cline(HTTP 10.77.0.2:3457, overview 单端点, 缓存30s/60s+dedup)
                      └─ LLM detector / CPA / Vibe Code（低频）
```

## 链路 3：数据采集（后台，每 10 分钟）

```
124 NewAPI SQLite → 采集器 query_usage.py（每批重开 SQLite，流式分批，高水位游标，推送不压缩）
                  → Japan Blog API /internal/newapi-usage/logs
                  → appendUniqueUsageLogs（去重；enrichment 时全量重写 NDJSON + 触发 SQLite 全量重建）
                  → 清空 usageResultCache/usageContextCache → 下一个页面访问付冷读成本
124 → aggregate_daily.py → 快照 → usage_snapshots(SQLite)，data-only 推送零 JSON 写 ✓
```

## 链路 4：Network probe / Beszel / VPN observer

```
probe → ingest → network-performance.sqlite（不再写 storage.json）✓
Beszel → monitoring-bridge.py（批量 SELECT）→ bridge.js(10s 缓存) ✓
Hysteria journal → observer.py（每 5s：journalctl json + export 双读双解析）→ observer SQLite
VPN Manager ← 每 30s 轮询（前端 useVpnPolling 30s single-flight）
```

---

# CONFIRMED BOTTLENECKS

## N1 NewAPI 日志 enrichment 全量重写 NDJSON + SQLite 全量重建级联 + 进程内存无界驻留

- **SEVERITY**: P0（唯一同时构成 OOM 风险与秒级延迟尖刺的问题）
- **USER IMPACT**: enrichment 发生时该次 ingest 请求阻塞数百 ms~数秒；随后所有 NewAPI 用量页冷读全量重建；`cachedRows` 一旦装载全量历史（当前生产约 46–48MB/87k 行，且持续增长）永不释放，在 1GB 内存节点上挤出 swap（已有 286–302MB swap 使用记录）
- **HOT PATH**: 采集 ingest（10 分钟一次）+ 其后任意用量页请求
- **FILES**: `backend/src/services/newapi/newapi-usage-log-store.js`、`backend/src/services/newapi/newapi-usage-sqlite-store.js`
- **FUNCTIONS**: `appendUniqueUsageLogs`、`readUsageLogSnapshot`、`readUsageLogs`、`rebuild`
- **CURRENT BEHAVIOR**:
  - 任意 enrichment（重复日志回填 groupName/tokenName/ip/quotaUnits）→ 先全量装载历史（`newapi-usage-log-store.js:718-723`），再整体序列化重写 NDJSON（`:742-744`，`fs.writeFileSync` 全文件），随后 `invalidateSqliteUsage`（`:746`）
  - SQLite 失效 → 下一次范围读触发 `rebuild()`（`newapi-usage-sqlite-store.js:423-445`）：整个 NDJSON readFileSync + 逐行 JSON.parse + 单事务全量 INSERT，全部在 HTTP 请求线程内
  - `readUsageLogs` 的 `cachedRows` 无 TTL、无条目上限、无驱逐（`:20-28, 584-641`）；enrichment 的 `markChange('full', nextRows…)`（`:776-777`）还长期持有整份历史引用
- **WHY IT IS SLOW**: 成本 = O(全文件) 序列化写 + O(全文件) parse + O(全表) 插入，且三者级联发生；内存驻留 O(全文件) 永不回收
- **MEASURED / INFERRED**: 本机 100k 行（29.5MB）全量冷读 p50 92.6ms（bench-newapi-usage）；生产 87k 行 + 日本机器 + 重写与重建叠加 → 每次 enrichment 秒级阻塞为合理推断；内存驻留 ≈ 文件大小 ×2–4（对象开销），按当前增速数月内逼近 1GB 节点承载上限
- **EVIDENCE**: 上述行号；`newapi-usage.js:816-830` ingestLogs→appendUniqueUsageLogs→clearUsageResultCache；compose 确认 `BLOG_NEWAPI_USAGE_SQLITE="1"`
- **EXPECTED IMPROVEMENT**: enrichment 改为 SQLite UPDATE（或 append-only enrichment 侧表），NDJSON 退役为只追加主日志/备份；`cachedRows` 设行数上限。消除 OOM 风险与秒级尖刺，稳态 ingest 恒定毫秒级
- **RISK**: 需要迁移策略（SQLite 已在生产启用，实际是"NDJSON 降级为备份"，行为变化点在去重与恢复路径）；中
- **RECOMMENDED CHANGE**: ① `appendUniqueUsageLogs`：enrichment 走 SQLite `UPDATE usage_logs SET raw_json=...`；无 SQLite 时才回退全量重写。② `invalidateSqliteUsage` 仅在确实绕过 SQLite 写文件时调用。③ `cachedRows` 加 `MAX_CACHED_ROWS`（如 50k）+ 超限自动降级为 SQLite 键查找路径。④ 补齐 enrichment-only 的回归测试

## N2 公共首屏热路径上的 eventStats 全表聚合 + interaction_events 无 retention

- **SEVERITY**: P1（当前可感知、随时间线性恶化的公共路径问题）
- **USER IMPACT**: 每次 `/api/public/site`、`/api/public/home` 边缘缓存未命中都执行一次全表 4 聚合 COUNT；10 万事件时本机 p50 17.7ms（日本估计 30–50ms），事件无上限增长，未来成为公共首屏的主要 Node CPU 项
- **HOT PATH**: 公共博客首屏（最高频 GET）
- **FILES**: `backend/src/lib/analytics-store.js`、`backend/src/routes/public.js`
- **FUNCTIONS**: `eventStats`（analytics-store.js:464-503）、`publicDerived`（public.js:156-172）
- **CURRENT BEHAVIOR**: `publicDerived` 缓存只覆盖 tagCloud/contentStats（keyed by db 身份+updatedAt），stats 部分每次请求现算：`eventStats` → 每次新开 SQLite 连接（`withDatabase`，:70-134）→ `SELECT COUNT(*), SUM(CASE...), COUNT(DISTINCT...) FROM interaction_events`（:468-475，无 WHERE，索引帮不上）；全库 grep 确认 interaction_events **无任何 retention/prune**
- **WHY IT IS SLOW**: O(全表) 聚合 × 每请求 + 连接建立开销；数据只增不减
- **MEASURED / INFERRED**: 本机临时脚本实测 100k 事件 → `eventStats` 单次 p50 17.69ms / p95 19.94ms；5k 事件时 bench-public-derived 的 cached 路径 2.45ms 中大头即此
- **EVIDENCE**: public.js:141、169 两处调用；analytics-store.js:468-475；grep 无 `DELETE FROM interaction_events`
- **EXPECTED IMPROVEMENT**: 进程内 20–30s 缓存（与 lib/analytics.js 的 20s TTL 同构）→ 热路径 ≈0ms；加 retention（如 180 天）封顶表大小
- **RISK**: 低（统计允许 20–30s 滞后；retention 需要数据策略确认）
- **RECOMMENDED CHANGE**: ① 在 public.js 包一层 TTL 缓存或把 stats 并入 publicDerivedCache（独立失效周期）。② 增加 `interaction_events` 定期清理（复用现有 retention 模式，如 observer.py:624-631 的做法）

## N3 VPN Manager `apply_store()` 无条件执行 traffic snapshot（怀疑区 C：证实）

- **SEVERITY**: P1（收益/成本比最高的单点修复）
- **USER IMPACT**: 任何 VPN 管理变更（含纯 egressMode 切换、甚至无效果的重命名 PATCH）都要先跑一个 Python 子进程做流量快照，timeout=12s；期间管理请求阻塞
- **HOT PATH**: PATCH `/v1/devices/egress`（批量）、PATCH `/v1/devices/:id`、设备增删/轮换
- **FILES**: `backend/deploy/sxyq27-vpn-manager.py`
- **FUNCTIONS**: `apply_store`（:650-690）
- **CURRENT BEHAVIOR**: `changed_configs` 在 ：661-664 已可判断；但 traffic snapshot（`:669-676`）在 try 块内、判断是否需要重启之前无条件执行（只要 Hysteria 服务 active）。而代码注释自证（`:435-438`）："The selected egress mode only controls which endpoint a newly fetched subscription advertises" —— **egressMode 完全不影响渲染出的 Hysteria 配置**，纯 egressMode 变更 `changed_configs` 恒为空、无需重启，snapshot 纯浪费
- **WHY IT IS SLOW**: 子进程 spawn（Python 解释器启动）+ 快照脚本执行（读 trafficStats API + 写库），0.2s~12s
- **MEASURED / INFERRED**: 快照脚本本体在服务器 `/usr/local/sbin/sxyq27-hysteria2-traffic-snapshot`（仓库外），实测时长未知；timeout=12s 为代码级上限；至少子进程启动 + journal/counter 读取为确定成本
- **EVIDENCE**: :669-676（无条件 snapshot）、:661-664（changed_configs 先算）、:435-438（egressMode 与配置无关）、:1224-1262（批量 egress 走 apply_store）、:1274-1302（单设备 PATCH）
- **EXPECTED IMPROVEMENT**: 纯 egressMode 切换从"快照+写库"降为"写 devices.json"，延迟从 0.2–12s → 毫秒级
- **RISK**: 低（仅需把 snapshot 移入 `changed_configs` 非空分支；凭证变更路径行为不变）
- **RECOMMENDED CHANGE**: `if changed_configs and service_active(): snapshot…`；其余流程不动。注意：设备密码轮换等会改配置的场景仍需 snapshot（Hysteria 重启会丢内存计数器）

## N4 NewAPI 用量查询无 SQL pushdown：JS 大数组 filter/sort/paginate + overview 多遍重复扫描（怀疑区 A：部分证实）

- **SEVERITY**: P1（当前量级可承受，是随日志量线性恶化的最大代码项）
- **USER IMPACT**: 每次采集（10 分钟）清空全部结果缓存后，下一个打开用量页的请求付全范围冷读；30d 范围时为"SQL 读全范围行 + 5~7 遍 JS 全量扫描"
- **HOT PATH**: `GET /api/admin/newapi-usage/*`（overview/logs/tokens/profiles）
- **FILES**: `backend/src/services/newapi/newapi-usage.js`
- **FUNCTIONS**: `usageContext`（:1331-1366）、`newestLogsForContext`（:1368-1375）、`listUsageLogs`（:2028-2097）、`buildUsageOverview`（:1873-1911）
- **CURRENT BEHAVIOR**:
  - `usageContext` 把范围内**全部行**读入 JS（`MAX_LOG_QUERY_ROWS=1_000_000`，:33、:1341）；context 缓存只收 ≤2000 行（`MAX_CACHED_USAGE_CONTEXT_ROWS`，:1354-1364）→ 30d 大范围每次冷读都重做
  - `listUsageLogs`：9 段串行 `.filter`（:2058-2067）+ 全量 sort（:1368-1375）+ `rows.length` 当 COUNT（:2070）+ slice 分页（:2081）——model/token/channel/group/type/status 全部 JS 过滤，SQLite 只承担时间窗+channel+LIMIT（sqlite-store.js:506-570）
  - `buildUsageOverview`：`summarizeLogs`、`timelineFromLogs`、`logBreakdown`×2、`errorBreakdownFromLogs`×2——其中 **errorBreakdownFromLogs 在 ：1898 与 ：1904 被重复调用**，纯浪费一遍全量扫描
  - 结果缓存 5min TTL（:45-47）但签名含 NDJSON mtime/size（:216-225），每次 ingest 全清（:825-828、:892-893）
- **WHY IT IS SLOW**: COUNT/filter/order/pagination 留在 JS；范围行全量进内存；多遍重复扫描
- **MEASURED / INFERRED**: 本机 100k 行：SQLite 24h 范围读 p50 13.97ms（NDJSON 索引路径 20.13ms、全量冷读 92.63ms）——SQL 读层本身已合格，**慢在 SQL 之上的 JS 层**；30d≈25k 行 × 多遍扫描 ≈ 数十 ms/遍（本机），日本 ×2–4
- **EVIDENCE**: 上述行号；compose `BLOG_NEWAPI_USAGE_SQLITE="1"`
- **EXPECTED IMPROVEMENT**: logs 列表的 filter/COUNT/sort 下推 SQLite（`usage_logs` 已有 created_at/channel 索引，需补 model/token 相关列或生成列）后，30d 列表查询从"全范围读入+多遍扫描"降为"SQL 返回当页 30 行 + SQL COUNT"；overview 双重 errorBreakdown 去重立即省一遍扫描
- **RISK**: 中（聚合口径必须逐字段对齐；建议先只做 COUNT 下推与双重扫描去重这两个低风险项）
- **RECOMMENDED CHANGE**: 第一步（低风险）：`errorBreakdownFromLogs` 计算一次复用；`listUsageLogs` 的 total 用 SQL `COUNT(*)` 替代全量 filter。第二步（独立评审）：按维度的 SQL 化过滤与分页

## N5 VPN Manager 读端点每请求 4 子进程 + 2 HTTP + 全量 observer SQL，零缓存

- **SEVERITY**: P2
- **USER IMPACT**: Admin VPN 页 30s 轮询的每个请求，manager 都现跑 `systemctl is-active`×2 + `ss -lun`×2 + trafficStats HTTP×2 + observer 遥测 SQL（12 台设备 30–80ms/请求）；manager 与前端同一 30s 节奏，白做大量重复工作
- **HOT PATH**: GET `/v1/status`、`/v1/overview`、`/v1/devices`（manager.py:1029-1086 → `status_payload` :800-851 → `service_active` :588、`udp_port_listening` :596-622、`aggregate_traffic`/`traffic_api` :712-741）
- **MEASURED / INFERRED**: 子代理审计 + 本地代码走读；12 台设备下量级毫秒级，属"白干的功"而非用户可感延迟
- **RECOMMENDED CHANGE**: status/overview/devices 共享一个 5–10s TTL 的进程内缓存（single-flight），约 20 行 Python

## N6 NewAPI 采集推送不压缩 + 每批重开 SQLite

- **SEVERITY**: P2（首次全量回填时放大 5–10×；稳态为小浪费）
- **USER IMPACT**: 跨 124→Japan WireGuard 链路的推送字节放大；`--all-history` 导入可达数百 MB 未压缩；稳态每 10 分钟 0.4–0.7MB×放大
- **FILES**: `ops/newapi-usage/query_usage.py`、`ops/newapi-usage/aggregate_daily.py`
- **CURRENT BEHAVIOR**: 推送无 `Content-Encoding: gzip`（服务端已支持解压，server.js:44-57）；`read_log_batch` 每批新开 SQLite 连接（query_usage.py:662-673，注释明示 "close SQLite before the caller uploads"）
- **RECOMMENDED CHANGE**: 推送端 gzip（与 aggregate_daily 的压缩先例对齐）；iter_log_batches 循环外保留一个只读连接

## N7 Admin 首屏 Element Plus 全量进入口

- **SEVERITY**: P2（只影响 admin，个人后台可接受度较高）
- **USER IMPACT**: admin 入口携带 Element Plus 完整组件库样式与全量图标注册；公共站点不受影响（fe-audit 确认公共侧无 element-plus/echarts 静态引入，路由全懒加载，Mermaid/KaTeX/ECharts 均按需 dynamic import）
- **FILES**: `frontend/src/admin/bootstrap/admin-ui.js`、`frontend/vite.config.js`
- **MEASURED / INFERRED**: 源码级确认（子代理 fe-audit，file:line 见其记录）；本轮未跑 build 实测 chunk 尺寸。`chunkSizeWarningLimit=1200` 只是告警配置，不是优化
- **RECOMMENDED CHANGE**: Element Plus 改按需（unplugin-vue-components）或维持"按页面类别动态加载控件组"的既有设计并把 icons 改按需注册；优先级低于后端项

## N8 VPN observer 每 5 秒双读 journal（json + export 两遍）

- **SEVERITY**: P2
- **USER IMPACT**: 无直接用户影响；每 5s 两个 `journalctl` 子进程 + 同一批条目两遍解析（~31,500 次子进程/天），其中 export 遍仅首次回填有意义（`journal_export_backfill_v1` 置位后仍每轮执行 export）
- **FILES**: `backend/deploy/sxyq27-vpn-observer.py` `ingest_journal`（:524-621，双读在 ：528-535 与 ：607-613；backfill 标志只改参数不改"是否执行"，:550-551）
- **RECOMMENDED CHANGE**: backfill 置位后跳过 export 遍；或把采样间隔放宽到 10–15s

## N9 Dashboard overview 每请求全窗口加载 + 20s 缓存成死代码

- **SEVERITY**: P3
- **FILES**: `backend/src/routes/admin.js:2604-2632`、`backend/src/lib/analytics.js:7-26`
- **CURRENT BEHAVIOR**: `/api/admin/analytics/overview` 直接 `listAllEvents({from,to})` 全窗口行（含 payload/user_agent）进 JS 再聚合（`limitWindow:false`），每请求现算；`computeAnalyticsOverviewCached`（20s TTL）只被 bench 引用，HTTP 路由未接。事件量小的时候无感，是 interaction_events 无 retention 的又一个受益点
- **RECOMMENDED CHANGE**: 路由改用 `computeAnalyticsOverviewCached` 同构缓存（事件来源于 SQLite，需要把缓存 key 加上 events 数据签名或走短 TTL）；配合 N2 的 retention

---

# SUSPECTED BOTTLENECKS

- **S1 公网链路主导首屏延迟（部署域，非代码）**：2026-09-25 实测公网 /home API 1.34–1.57s、日本本机 8.9ms、124→Japan WireGuard 前端监听 0.213–1.702s 且 ping 50% 丢包（trellis 09-02 research 2026-09-24/25 记录）。**这不是 Node 慢，是 124 中转路径慢**。若公网流量改走 Japan 直连入口（`blog-frontend-japan-public-http.nginx.conf` 样例已存在），首屏可省约 1s+ —— 单项收益超过其余所有代码优化之和。需部署评审，本轮只记录。
- **S2 管理端 API 响应无应用层 gzip**：server.js 无 compression 中间件（server.js 全文核实），依赖 Nginx gzip；未实测生产响应头，故不入 CONFIRMED。
- **S3 usage 页 30d 冷读在 50 万行后的退化速率**：当前 87k 行量级冷读可承受；50 万行时 30d 范围 ≈ 43 万行进 JS，即便 SQL 读层也会到数百 ms，配合 N4 的 pushdown 才有解。规模未到，先记录。
- **S4 admin `/analytics/events` 的 LIKE 搜索**：`buildWhere` 的跨列 LOWER(...) LIKE 无法用索引（analytics-store.js:254-262），大表后退化为全表扫描；当前量级无感。

---

# ALREADY SOLVED（历史大问题逐项核实，当前 HEAD 确认已解决）

| 历史问题 | 当前状态 | 证据 |
| --- | --- | --- |
| 100+MB storage.json | ✅ 本地实测 258KB；三类高频数据已迁 SQLite | `data/storage.json`；migrate-*.js 三套迁移脚本 |
| interactionEvents JSON 持久化（track 全量重写） | ✅ `/track` → analytics.sqlite 追加 | public.js:280-299；appendEvent analytics-store.js:423-443 |
| networkPerformanceSamples JSON 持久化 | ✅ 专用 SQLite，ingest 不触 storage.json | network-performance-store.js（子代理复核） |
| NewAPI snapshot JSON 持久化 | ✅ usage_snapshots 表；data-only 推送零 JSON 写，仅 pricing 才写 | newapi-usage.js:832-894（:886-891 条件 writeDb） |
| /home //site 每请求全文章 stripMarkdown 重算 | ✅ publicDerivedCache | public.js:145-172；实测 45.2ms→2.45ms p50 |
| NewAPI 冷路径全历史解析 | ✅ 偏移索引 + SQLite 范围读 | 实测 92.6→14.0ms（24h, 100k 行） |
| NewAPI 用量页重叠请求风暴 | ✅ 结果缓存+签名失效+X-Refresh 语义 | newapi-usage.js:227-237 |
| 监控页重叠请求 | ✅ single-flight | 子代理 fe/routes 复核 |
| Beszel 概览 201 次 SELECT | ✅ 批量化 3 次 | monitoring-bridge（ops-audit 复核在位） |
| VPN 在线设备 200 次 SELECT | ✅ 按用户批量 2 次 | sxyq27-vpn-observer.py（ops-audit 复核） |
| 采集器全量列表构造/双查询 | ✅ 流式分批+跨日单查询+高水位游标（50k 日志峰值 3.61MiB） | query_usage.py:616-659 |
| ECharts/Mermaid/KaTeX eager load | ✅ 全部 dynamic import 按需 | echarts-runtime.js / mermaid-renderer.js（fe-audit） |
| 路由非懒加载 | ✅ public/admin 路由全懒加载 | router/*.js（fe-audit） |
| 公共首页请求风暴 | ✅ 单一 /api/public/home 聚合；PublicLayout 在 home 跳过重复 /site | fe-audit 复核 |
| Cline 重复调用/大响应 | ✅ overview 单端点 + 30s/60s 缓存 + inflight dedup + HTTP/1.1 pin（最新提交） | routes-audit 复核，结论"不建议优化" |
| VPN 前端重复轮询 | ✅ useVpnPolling 30s single-flight | fe-audit 复核 |

---

# NOT WORTH OPTIMIZING（当前规模下明确不建议动）

- **store.js 全量 JSON 写（writeDb/saveDb）**：258KB → stringify+rename 约 2–5ms；调用方仅剩 admin CRUD、渠道设置、pricing（低频）。继续迁 SQLite 属过度工程，保持 JSON。
- **refreshDerivedData 每次写重建搜索索引**：~300 篇文章毫秒级、低频写路径。
- **getPublishedArticles/sortRecords 每请求排序**：~300 条，微秒级。
- **/articles 与 /search 的全文 includes 扫描**：60s 边缘缓存 + 300 篇规模。
- **richTextCache（50 条上限，键含内容哈希）**：有界、命中即免渲染，合理。
- **Cline 链路**：缓存、dedup、单端点、超时齐备，无实测问题——明确"不建议优化"。
- **VPN observer 5s 采样/12 台设备**、**monitoring bridge 10s 缓存**：规模匹配。
- **rate limiter 内存 Map、bucketFormatters、Server-Timing 中间件**：常数开销。
- **toPublicArticle 的 deepClone**：单篇文章级。
- **aggregate_daily 跨日聚合耗时 +5.3%**：历史已记录的已知权衡（换内存减半），不再动。
- **chunkSizeWarningLimit=1200**：告警阈值配置，不是性能手段。
- ** bench-synthetic.js 跑挂**：脚本自身过期（track 迁 SQLite 后计时字段缺失，bench-synthetic.js:242），属测试工具腐化，非产品性能问题；下轮修 bench 而非产品。

---

# PRIORITY

| ID | 问题 | 预计收益 | 实现复杂度 | 风险 |
| --- | --- | --- | --- | --- |
| **P0** | N1 enrichment 全量重写+重建级联+无界内存 | 消除 OOM 风险与秒级尖刺；稳态 ingest 恒定化 | 中 | 中（需迁移与回归） |
| **P1** | N2 eventStats 热路径 + retention | 公共首屏未命中请求 17–20ms→≈0，封顶退化 | 低 | 低 |
| **P1** | N3 apply_store 条件快照 | VPN 策略切换 0.2–12s→毫秒级 | 极低（1 行判断） | 低 |
| **P1** | N4 SQL pushdown 第一步（COUNT 下推+双重扫描去重） | 30d 列表/overview 冷读线性改善 | 低~中 | 低（第一步口径不变） |
| **P2** | S1 公网改走 Japan 直连（部署评审项） | 首屏 -1s 级，全场最大单项 | 部署级 | 中（入口切换） |
| **P2** | N5 manager 读缓存 5–10s | 轮询白工 -90% | 极低 | 低 |
| **P2** | N6 采集 gzip + 连接复用 | 回填带宽 -5~10× | 低 | 低 |
| **P2** | N7 Element Plus 按需 | admin 首屏 chunk 减半量级 | 中 | 低 |
| **P2** | N8 observer export 遍跳过 | 每日子进程减半 | 极低 | 低 |
| **P3** | N9 dashboard 缓存接入 / N10–N12（network_quality N+1、audit 无轮转、per-batch 重连） | 小幅 | 低 | 低 |

---

# 最终结论：如果只允许再做 3~5 项

1. **N1：NewAPI 日志存储治理**（P0）——把 SQLite 立为读取与更新主存储，NDJSON 退为只追加日志；enrichment 走 UPDATE；`cachedRows` 加上限。这是唯一一个同时威胁内存安全（1GB 节点 + 无界驻留 + 已见 swap）和延迟（秒级重写+重建级联）的问题，且数据还在按每 10 分钟一批增长。
2. **N2：公共首屏 eventStats 缓存 + interaction_events retention**（P1）——两处小改动保住最高频的公共路径，并给所有依赖该表的路径（含 N9）封顶退化。
3. **N3：apply_store 条件化 traffic snapshot**（P1）——一行判断，直接把 VPN 策略切换从"可能 12 秒"变成毫秒级，是全场性价比最高的修复。
4. **N4 第一步：listUsageLogs 的 COUNT 下推 + overview 双重 errorBreakdown 去重**（P1）——低风险地扭转"全范围进 JS"的趋势，为将来数据量翻倍留出余量。
5. **S1（部署评审，不属代码轮）**：评估公网流量改走 Japan 直连入口。它不是代码问题，但对最终用户延迟的影响（约 -1s）大于以上全部代码优化之和。

第一阶段审计到此为止：未改任何产品代码、未部署、未动配置。等 Review 决定下一轮实现范围。
